# 한 줄 작곡 — 글로 만드는 AI 음악

“요즘 K팝 걸그룹 타이틀곡”처럼 한 줄 적으면 장르·조성·템포·악기를 읽어 곡을 짓고,
브라우저가 바로 연주한다. 결과는 WAV(오프라인 렌더)와 MIDI(악기별 트랙)로 저장한다.

기본 목표는 2020년대 중반 K-pop 이다. 이지리스닝 댄스팝에 프로그레시브 하우스를 섞고,
후렴 직전에 반주를 끊었다가 내리꽂고, 후렴에서는 드럼을 앞세우고 멜로디는 단순하게 간다.

- 배포: https://richroro.github.io/ai-music/
- 소스: `index.html` 한 파일 (외부 스크립트·폰트·샘플 없음, 서버 없음)

## 솔직한 정의

학습된 신경망이 아니다. **음악 이론 규칙 + 확률(마르코프 체인·가중 추첨)** 로 작곡하는 엔진이고,
소리는 Web Audio 오실레이터·필터·노이즈로 그 자리에서 합성한다.
Suno·Udio처럼 목소리가 들어간 노래는 못 만든다. 대신 설치·가입·비용이 없고, 같은 문장·씨앗값이면
언제 어디서나 같은 곡이 나오며, MIDI로 가져가 DAW에서 손볼 수 있다.

Magenta.js(MusicRNN) 같은 브라우저용 신경망을 붙이는 것도 검토했지만, 체크포인트를 외부 CDN에서
내려받아야 하고(수십 MB) 그 CDN이 언제 사라질지 모른다. “한 파일, 의존성 0” 원칙을 지키는 쪽을 택했다.

## 되는 것

| 영역 | 내용 |
| --- | --- |
| 입력 | 한국어·영어 자유 문장. `120bpm`, `A 단조`, `다장조`, `2분` 같은 직접 지정도 읽는다 |
| 장르 | **K-pop**·발라드·팝·록·재즈·로파이·힙합·EDM·8비트·시네마틱·앰비언트·보사노바·왈츠(3/4)·시티팝·동요 (15종) |
| 해석 | 장르 점수, 밝기(valence)·에너지, 템포 가감, 선법(장·단·도리안·리디안·믹솔리디안·프리지안), 악기 지정, 5음 음계(국악), 독주(드럼·베이스 제거), 빗소리·파도 배경 |
| 작곡 | K-pop 은 인트로–절–**프리코러스**–후렴–**드롭**–아웃트로, 그 외는 인트로–절–후렴–브리지–아웃트로. 장르별 코드 진행 + 마르코프 생성, 성부 진행(voice leading), 2마디 통짜 후크의 이조·재작성·종지 변형, 장르별 베이스·반주·드럼 패턴, 필인·크래시, 스윙 |
| 소리 | 악기 28종(K-pop 플럭·보컬 찹·808·피아노·스트링·슈퍼소우·패드·8비트 펄스·벨·색소폰·베이스 8종…), 미리 구워 둔 드럼 킷, 전환 효과음(라이저·임팩트·스웰) |
| 믹스 | 스테레오 스테이지(음역별 코드 레인·드럼 킷 팬·유니즌 신스), 플레이트+홀 잔향, 핑퐁 딜레이, 사이드체인, 글루→드라이브→소프트 클리퍼→리미터 마스터, 섹션을 따라가는 필터·음량·폭 자동화 |
| 재생 | 재생/일시정지/처음으로, 진행바·피아노롤 클릭으로 탐색, 스페이스바, 재생 위치 따라 피아노롤 자동 스크롤 |
| 손보기 | 템포·조성·선법·길이·스윙·드럼 유무·씨앗값. 바꾸면 즉시 다시 작곡 |
| 저장 | WAV 44.1kHz 16bit 스테레오(피크 정규화), MIDI SMF 1(템포·박자·조표, 악기별 트랙·GM 프로그램, 드럼은 10번 채널), 링크 복사(`#p=문장&s=씨앗…`) |

## 구조

`문장 → 설계도(P) → 악보(song) → 이벤트(초) → 소리` 네 단계가 한 방향으로만 흐른다.

```
interpret(prompt, seed, overrides)  →  P   { genre, bpm, root, mode, beats, swing, lead, chordInst, … }
compose(P)                          →  song { sections, chords, lead, chords_tr, bass, pad, arp, drums }   (박 단위)
renderEvents(song)                  →  song.events  [{ time(초), d, p(MIDI), v, tr, inst }]  (스윙·휴먼라이즈 적용)
buildGraph(ctx) + playEvent()       →  실시간 AudioContext 또는 OfflineAudioContext (같은 코드)
```

### 1. 문장 해석 (`LEX`, `interpret`)

- 78개 항목·500여 개 어간의 단서 사전. 각 항목은 어간 목록(`["잔잔","차분","calm",…]`)과 효과(장르 점수, 밝기, 에너지, 템포, 선법, 악기, 플래그)를 갖는다.
  조사·활용을 따로 처리하지 않고 **부분 문자열**로 맞춘다(“잔잔한”, “잔잔하게” 모두 “잔잔”에 걸린다).
- 장르는 점수 최댓값. 동점이면 씨앗값으로 고른다. 단서가 없으면 밝기·에너지로 후보군을 좁힌 뒤 씨앗값으로 고른다.
- 씨앗값은 `FNV-1a(문장) XOR (씨앗번호 × 황금비 상수)`. 같은 문장·번호면 같은 곡, “🎲 다른 버전”은 번호만 올린다.
- 화면의 “→ 「비」 비 오는 날 감성으로…” 목록이 실제로 걸린 단서다. 해석이 이상하면 이 목록에서 어느 단어 탓인지 바로 보인다.

### 2. 작곡 (`compose`)

- **구조**: 목표 길이(짧게 30초 / 보통 70초 / 길게 140초)를 템포·박자로 나눠 마디 수를 구하고, 8마디(짧으면 4마디) 섹션을
  `A B`, `A B A B`, `A B A B C B`… 순으로 채운다. 느린 발라드와 빠른 8비트가 같은 “보통”을 골라도 실제 길이가 비슷하다.
- **화성**: 절·후렴·브리지마다 진행을 한 번 정해 재사용한다(반복되는 절은 같은 코드). 70%는 장르별 진행표에서, 30%(브리지는 60%)는
  마르코프 전이표에서 뽑는다. 섹션 끝은 55% 확률로 딸림화음으로 바꾸고, 팝·발라드는 마지막 마디를 IV→V로 쪼개 종지를 만든다.
  단조의 V는 장르별 확률로 장3화음(화성단음계)이 된다. 7화음 확률도 장르별(재즈 100%, 록 0%).
- **보이싱**: 코드음마다 55~76 사이 옥타브 후보를 전부 조합해 “이전 보이싱에서 가장 적게 움직이는 것”을 고른다(성부 진행).
- **멜로디**: 이 부분을 한 번 갈아엎었다. 첫 판은 16분음표를 빈틈없이 채우는 리듬 셀을 썼는데,
  뽑아 보니 쉼표가 18~22%뿐이고 당김음이 **0**이었다 — 쉬지 않고 흥얼대는, 외울 수 없는 선율.
  지금은 **2마디 통짜 후크를 문자열로 적는다**(`x`=음 시작, `-`=이어짐, `.`=쉼표):

  ```
  "x-x-x---x-x-x---x-x-x-------...."   ← K-pop 후렴
  "..x-x-x---......x-x-x-----------"   ← 당김음으로 들어가는 절
  ```

  눈으로 보고 고칠 수 있어서 "구절 끝을 비운다", "센박을 피해 들어간다" 같은 규칙을 지키기 쉽다.
  음높이는 강박=코드음, 약박=순차 진행을 선호하는 가중 추첨으로 붙이되 **5음 음계에 무게를 싣고**
  (팝 계열은 5음 밖 가중치 0.32~0.5), 후크 계열은 **같은 음을 두 번 치는 걸 오히려 권한다**.
  8마디는 `동기 → 이조/재작성 → 동기 → 종지 변형` 네 구절이고, 동기 폭은 4도 안으로 묶는다.
- **섹션 대비**: 절은 음역 아래쪽(0.30 지점)에서 성기게, 프리코러스는 구절마다 한 도씩 올라가며
  딸림화음으로 열어 두고, 후렴은 음역 위쪽(0.70)에서 한 단 촘촘하게. 드롭은 한 마디 리프를 통째로 반복한다.
- **후렴 직전 컷**: 프리코러스 마지막 두 박에서 반주·드럼을 전부 지우고 클랩 한 방만 남긴 뒤,
  2마디 라이저로 끌어올려 후렴 첫 박에 임팩트를 얹는다.
- **반주**: 장르별 스타일 키 하나로 분기한다 — 아르페지오(발라드), 블록(팝), 컴핑(재즈, 찰스턴 리듬 추첨), 스트럼, 스탭(EDM 뒷박),
  16분 아르페지오(8비트), 움파(왈츠), 서스테인(시네마틱·앰비언트), 파워코드 8분(록), 로파이 두 번 치기(9음 추가 35%).
- **베이스**: 루트·5도·옥타브·3도·접근음 역할 표기로 패턴을 적고, 워킹 베이스는 `루트 → 3/5도 → 경과음 → 다음 근음의 반음 접근`.
- **드럼**: 16스텝 확률표(3박자는 12스텝). 1.0은 항상, 그 아래는 섹션 강도(절 .85, 후렴 1, 브리지 .75)를 곱해 추첨.
  섹션 마지막 마디는 80% 확률로 필인(스네어→탐, EDM 스네어 롤, 시네마틱 팀파니 롤), 후렴 첫 박 크래시, 아웃트로 첫 박에 마지막 한 방.

### 3. 소리 (`INST`, `DRUM`, `buildGraph`)

- 악기 함수 하나가 음 하나다: `(g, out, f, t, d, v)`. 오실레이터 1~5개 + 게인 ADSR + 필터. 감쇠는 `setTargetAtTime`(지수)로,
  릴리스 뒤 실제로 조용해지는 시각에 `stop()` 해 노드가 남지 않는다.
- 피아노는 삼각파+2배음 사인+약한 톱니를 저역 필터로 뭉치고 **음이 높을수록 빨리 사라지게** 했다. 일렉피아노는 2-오퍼레이터 FM(비율 1:1과 1:14).
  8비트 펄스는 `PeriodicWave`로 25% 듀티를 만든다. 벨·실로폰은 비정수 배음(2.756×, 5.4×)의 사인.
- 그래프: `트랙 게인 → (사이드체인) → 마스터 → [로파이: LPF 3.4k + 계단형 WaveShaper] → DC 차단 HPF → 컴프레서 → 출력`.
  잔향은 씨앗값으로 만든 지수 감쇠 노이즈 임펄스를 `ConvolverNode`에, 딜레이는 점8분 피드백. EDM은 킥마다 패드·아르페지오 게인을 0.22로 눌렀다 0.3초에 되돌린다.
- 실시간 재생은 120ms마다 0.5초 앞까지만 스케줄하는 lookahead 방식, WAV는 같은 `playEvent`를 `OfflineAudioContext`에 전부 스케줄한다.
  일시정지는 `ctx.suspend()`, 탐색은 그 시각 이후 이벤트만 다시 스케줄한다(그 전에 시작된 긴 음은 빠진다).

## 두 번째 업그레이드 — “음악이 너무 구리다”를 18단계로

첫 K-pop 판을 듣고 “더 최대한 업그레이드”하라는 요청을 받았다. 감으로 고치지 않고 이렇게 했다.

1. **감사** — 화성·멜로디·그루브·편곡·신스 음색·드럼 음색·믹스/마스터·성능·K-pop 다움, 9개 차원을 에이전트가 따로 읽고
   코드 인용과 측정으로 약점을 찾아 제안 44개를 냈다.
2. **반박 검증** — 제안마다 두 검토자가 “실제로 들리는 차이인가”와 “Web Audio 로 맞게 구현되는가” 관점에서 반박을 시도했다.
   35개가 살아남았고, 9개는 들리지 않거나(드럼 버스 컴프), 유행이 지났거나(마지막 후렴 반음 전조), 소리를 망쳐서(브리지 전체 로우패스) 버렸다.
3. **계획** — 살아남은 제안을 겹치는 함수끼리 묶고 충돌 20여 건을 정리해 18단계 순서로 만들었다.
4. **구현** — 단계마다 한 에이전트가 구현하고, 자동 게이트를 통과해야 커밋하고, 독립 검토자가 다시 측정했다.
   게이트는 15장르 × 3길이 음표 유효성, 결정성, 장르별 렌더(NaN·무음·피크), WAV·MIDI, 피아노롤 픽셀, 재생 제어, 모바일 폭을 본다.
   18단계 중 16단계가 한 번에 통과했고, 2단계는 검토자가 버그를 잡아 한 번씩 고쳤다.

### 전후 (K-pop “요즘 K팝 걸그룹 타이틀곡”, 헤드리스 크로미움)

| 항목 | 전 | 후 |
| --- | --- | --- |
| 전곡(79초) WAV 렌더 | 66초 | 6.7초 |
| L/R 상관 (1이면 모노) | 1.000 | 0.845 |
| side/mid | 0.012 | 0.29 |
| 음량 RMS (처음 30초) | -15.5 dB | -11.6 dB |
| 크레스트 | 5.52 | 3.23 |
| 절 → 후렴 음량 차 | 1.25 dB | 3.4 dB |
| 후렴 직전 저역 빠짐 | 15.6 dB | 31.8 dB |

### 단계별로 찾은 것

| 단계 | 증상 · 원인 · 해결 · 수치 |
| --- | --- |
| 1. 렌더 창 분할 | 증상: WAV 저장이 78초 K팝 한 곡에 1분 넘게 걸리고 진행률도 안 보였다. 원인: 렌더 전에 곡 전체 음표 약 7천 개의 노드를 한꺼번에 연결해서 곡이 길수록 제곱으로 느려졌다. 해결: OfflineAudioContext.suspend로 0.5초마다 멈추면서 앞으로 1초 안에 울릴 음만 연결하고, 진행률을 %로 보여준다. 수치: 66.4초 → 4.4초, 출력 RMS 차이 2e-8. |
| 2. 곡 구조 버그 | 인트로가 "A" 진행을 8개로 먼저 캐시해 두는 바람에, 4마디 섹션이 코드 8개를 다 깔아 뒤 화성이 16박씩 밀렸습니다. 그래서 짧은 곡(과 중간 길이 발라드·로파이·앰비언트)이 곡이 끝난 뒤에 V7/IV로 끝났습니다. 코드 수를 섹션 길이에 맞추도록(fitLen) 고쳐, 코드 타임라인이 곡 길이에 딱 맞는 곡이 286/450에서 450/450이 됐습니다. 긴 K-pop에는 브리지(C)를 넣었습니다. |
| 3. 드럼 킷 굽기 | 증상: 킥은 150→48Hz 사인 '붕' 소리(100ms에 -11dB), 스네어는 BP 노이즈, 햇·크래시는 흰 노이즈라 싸구려로 들리고 드럼이 모노(K팝 드럼 side RMS 0.002)였다. 해결: 드럼 음색을 JS로 한 번 합성해 AudioBuffer로 굽고(타격당 노드 2개) 808 금속 햇·넓은 클랩·몸통 있는 스네어로 바꿔 side RMS 0.039, 킥 150Hz-1kHz +3dB, 드럼 렌더 비용 523→306ms가 되었다. |
| 4. 섹션별 드럼 | 증상: K-pop 은 절·프리코러스·후렴 드럼이 같았고, 후렴 직전 2박 컷에도 패드·코드가 울려 멈춤이 들리지 않았음. 원인: DP.kpop 이 패턴 하나뿐이었고, 컷 전에 시작해 컷 안으로 이어지는 음을 자르지 않았음. 수정: 섹션별 드럼(스냅 절·하프타임 프리·4온더플로어 후렴)에 컷 앞에서 멈추는 스네어 빌드를 넣고, 컷 안으로 울리는 음을 잘랐음. 수치: 마디당 킥 A 2.7→2.0, B 2.9→3.9, 컷 안으로 울리는 음 68→0개. |
| 5. 드럼 루프 고정 | 증상: 드럼이 마디마다 확률을 새로 굴려 킥 패턴이 매번 바뀌고, 팝의 4분 하이햇이 약 23% 빠져 고장 난 랜덤처럼 들렸다. 원인: makeDrums의 play()가 매 스텝 rand.chance를 다시 불렀다. 해결: 섹션마다 한 번만 굴려 2마디 루프로 되풀이하고 네 번째 마디 마지막 박만 새로 굴린다. 16분 강약 윤곽과 고스트 스네어도 넣었다. 수치: b와 b+2 마디 일치율 팝 16%→100%, 4분 햇 유지율 77%→100%. |
| 6. 마스터 체인 | 킥이 8분음표 간격으로 붙으면 사이드체인이 .22에 붙어 있다가 한 샘플 만에 .78을 뛰어 딸깍 소리가 났고(EDM 최대 점프 .78), 6:1 마스터 컴프가 리드까지 통째로 펌핑시켰다. setTargetAtTime 덕킹(베이스 전용 버스 포함)과 메이크업을 상쇄한 글루→드라이브→소프트 클리퍼→리미터 체인으로 바꿔 점프를 .004로 줄였고, K팝 코러스는 RMS -13.4→-9.9 dBFS, 크레스트 12.3→8.5 dB로 조밀해졌으며 발라드·재즈는 클리퍼에 거의 닿지 않는다(0.02% 이하). |
| 7. 스테레오·공간 | 증상: 모든 악기가 모노로 가운데에 몰리고 잔향은 리드보다 약 20~30dB 작아서, 곡이 마른 모노 데모처럼 들렸다(K팝 코러스 side/mid -22.9dB, 발라드 리드 wet/dry -22.9dB). 원인은 팬·스테레오 폭이 없었고, ConvolverNode 정규화 때문에 리턴 레벨이 너무 낮았으며, 킥·베이스까지 잔향에 들어간 것. 코드 음역 레인·드럼 키트 팬·신스 코러스, 플레이트+홀 잔향(모노 IR 두 개를 컨볼버 하나에 담고 M/S 로 펼침), 핑퐁 딜레이를 넣어 K팝 side/mid -15.9dB, 발라드 리드 wet/dry -9.7dB 가 되었고, 모노 합산 차이는 0.23dB 이내, 렌더 시간은 15장르 합계 +5.5% 늘었다. |
| 8. 믹스 자동화 | 후렴 직전 빌드에서 저음이 충분히 안 빠지고(컷 직전 <200Hz 가 후렴 첫 박보다 15.6dB 낮은 정도) 리미터 때문에 벌스·후렴 음량 차가 1.3dB뿐이던 문제를, 리드·효과음을 뺀 반주 버스(band)에 하이패스 빌드(25→700Hz, 공진 -3→7dB, 후렴 첫 박에 복귀)와 섹션별 리미터 뒤 볼륨·드럼 리프트·폭 자동화(automateMix, 되감기 안전)를 넣어 고쳤다. 컷 직전 저음 차이는 31.8dB, 벌스→후렴 차이는 3.4dB가 되었고 렌더 시간은 약 3% 늘었다. |
| 9. 808·베이스 | 808 베이스가 킥과 따로 놀고(절 킥과 일치 0%), 마디마다 리듬이 바뀌며, 곡당 59번 저절로 미끄러지고, 2.6배 '퓽' 때문에 음정이 +227센트 틀렸습니다. 킥 루프 하나를 드럼과 808이 같이 읽게 하고, 리프는 섹션마다 한 번 고르고, 후렴은 뒷박 바운스, 808은 한 옥타브 아래로 내리고 짧은 노크를 넣었습니다. 그 결과 절 킥 일치 100%, K-pop 글라이드 5.6회, 음정 오차 +1센트이고 베이스 레벨은 ±1 dB 안입니다. |
| 10. 신스 음색 | K-pop 리드 kpluck 는 공진 필터의 '뿅' 소리와 ±7센트 두 톱니의 맥놀이 때문에 긴 훅 음의 기음이 -41dB 까지 꺼졌고, 슈퍼소우·패드·스트링은 모두 모노였다. 공진을 없애고 좌우로 나눈 디튠에 1/4 주기 늦춘 사인 몸통을 더했고(기음 최저 -41 → -15dB), 슈퍼소우·패드·스트링은 공용 unison() 으로 스테레오 유니즌이 되었다(side/mid -5dB, 스트링 멜로디 상관 .52). 렌더 시간 증가는 k-rate 필터로 +11% 안으로 줄였다. |
| 11. 멜로디 음역 | 증상: 키에 따라 K팝 후렴이 F6~A6까지 떠서 벨소리처럼 들렸고, 신나는 곡은 후렴이 8분음표 13연타였다. 원인: 음역을 도 단위로 잡은 데다 직전 음을 따라가는 계단식 상승과 '8비트·록' 리듬표였다. 수정: 섹션 음역을 C 기준 MIDI 높이로 고정하고 구절을 섹션 제자리에 붙였으며, 후렴 전용 한 마디 당김음 세포표(HOOKB4)를 추가했다. 결과: 'kpop' 후렴 평균 MIDI 71~89 → 71~78, 최고음 93 → 82, 2마디당 음수 13 → 9.2. |
| 12. AAAB 후크 | 후렴 후크가 코드가 바뀔 때마다 다시 맞춰져 같은 구절이 한 번도 그대로 반복되지 않았다(0/270). 드롭 리프도 마디마다 달라졌다. 후크를 반복될 코드 위에서 반음 위로 부딪히지 않게 짓고, 부딪힐 때만 고치게 해서 AAAB로 바꿨다. 후렴 반복 94%, 드롭 99.5%가 되었고 후렴 끝 ♭9 지속음은 122→0이다. K팝 후렴 직전 멈춤에는 16분 픽업 세 음을 넣었다(30/30). |
| 13. 보이싱 | 증상: K팝·재즈·로파이·시티팝 코드의 15~33%가 Cmaj7 = B3 C4 E4 G4처럼 반음이 붙은 뭉침이었고, 같은 보이싱이 슈퍼소 패드에도 들어가 탁했다. 원인은 voice()가 움직임만 최소로 잡고 음정 간격을 보지 않았기 때문이다. 해결: 반음 뭉침에 큰 벌점을 주고, 베이스가 도는 곳에서는 근음을 뺀 rootless 보이싱을 쓰고, 패드에는 근음을 맨 아래 둔 넓은 보이싱을 따로 두었다. 반음 뭉침은 전 장르 0%(K팝 14.7→0%), 패드 단2도는 184→0, 근음이 안 들리는 코드도 0%다. |
| 14. 코드 색 | 증상: 코드가 대부분 밋밋한 3화음이었다(팝·EDM 90%). K팝 V 는 절반가량이 딸림7이었고, 같은 루프도 돌 때마다 색이 바뀌었으며, 프리코러스 끝은 iv→v 로 흐지부지 끝났다. 원인: 코드 자리마다 7화음 여부를 동전으로 정했고, 마르코프 진행과 종지 덮어쓰기가 루프를 깼다. 해결: 곡마다 도별 색(add9·maj9·m9·sus4)을 한 번 골라 고정하고, 프리코러스·브리지 루프 표와 ii–iii/IV–V 계단을 넣었다. 수치: 팝 3화음 90.6→27.1%, K팝 V7 40.6→0%, 컷 직전 V 84/141→141/141. |
| 15. 편곡 시그니처 | K팝 후렴 밑에 8분음표 찹 아르페지오(곡당 128음)가 깔려 후렴 멜로디를 가렸고, 아웃트로는 으뜸화음을 2마디 끄는 데모식 페이드였다. 찹이 드롭 리프(마디마다 같은 모음)와 후렴 쉼표 속 대답만 맡게 해서 찹 음이 곡당 17.5개로 줄었고, 후렴 멜로디와 겹치는 음은 0개가 됐다. 버튼 엔딩은 마지막 마디 첫 박에 한 방으로 끊고(마지막 1.5초 -55 → -65 dBFS), 인트로는 후렴 코드 위에서 후크를 미리 들려준다(K팝 인트로-절 음량 차 19 → 7.4 dB). |
| 16. 스윙 | 스윙이 뒷박 근처(±.01) 음만 옮겨서, 조금씩 어긋나 치는 코드의 앞 두 음만 밀리고 순서가 뒤집히는 플램이 났다(재즈 82%, 로파이 37%). 박 안을 이어진 꺾은선으로 휘는 스윙 워프로 바꾸고 코드 한 번에 흔들림을 한 값만 줘서 0%가 됐고, 케이팝 스탭 번짐은 13.9ms에서 0ms로 줄었다. '트랩' 문장에는 셋잇단·32분 하이햇 롤이 붙는다(두 마디당 0에서 0.66회). |
| 17. 피아노 | 피아노가 한 번 줄어드는 단조로운 감쇠라 타악기 같은 어택과 긴 울림이 없었다(삼각파+사인+톱니 한 겹) → 세기별 PeriodicWave 두 줄(-0.5/+0.7센트, 둘째 줄 .4배), 프롬프트 감쇠(45%)·여운·댐퍼, 센 음에만 구워 둔 망치 소리로 바꿨다. 100ms 창 기음 골은 최대 0.1dB(같은 세기 두 줄 초안은 -51dB)이고, 피아노 트랙 음량은 ±0.4dB 안이며, 코드 트랙 렌더는 5~22% 빨라졌다. |
| 18. 실시간 여유 | 재생 중 raf가 매 프레임 피아노롤 캔버스 전체(폰 dpr3에서 7680px 폭)를 다시 그려서 프레임당 평균 2.6 ms(최대 24 ms)를 스케줄러와 같은 주 스레드에서 쓰고 있었습니다. 재생선을 transform으로 움직이는 2px DOM 막대로 바꾸고 레이아웃 읽기를 쓰기보다 먼저 하도록 해서 0.15 ms로 줄였고, latencyHint 'playback'으로 출력 버퍼를 키우고(baseLatency 0.010→0.023 s) 그 지연만큼 재생 위치를 보정했습니다. |

## 만들면서 걸린 것

| 증상 | 원인 | 대응 |
| --- | --- | --- |
| 80초 곡 WAV 렌더에 118초, 렌더 결과 RMS가 피크와 거의 같음 | 공용 `finish()`가 `start()`를 시각 없이 불러 **모든 오실레이터가 0초부터** 돌았다 | `start(t)`. 렌더 14초로, 크레스트 팩터 1.2 → 3.5~5.8 |
| 비브라토 악기에서 `cannot call start more than once` | LFO를 `vibrato()`에서 시작하고 `finish()`가 또 시작 | 시작은 한 곳에서만 |
| 62bpm 로파이의 “보통”이 2분 27초 | 구조를 마디 수로 고정해 템포가 느릴수록 길어짐 | 목표 초를 템포·박자로 나눠 섹션 수를 정한다 |
| **멜로디가 흥얼거려지지 않음** | 16분음표를 빈틈없이 채우는 리듬 셀 — 쉼표 18~22%, **당김음 0** | 2마디 통짜 후크 문자열로 교체. 쉼표가 생기고 당김음 0 → 0.42~0.58 |
| **나(B)장조 곡이 다장조보다 한 옥타브 높이 뜸** | `degToMidi` 가 `60 + P.root` 로 조성을 그대로 더해서, 같은 음역 설정이 키마다 딴 음역 | 조성을 C 주위로 접는다(`root > 6 → root - 12`). 최고음 97 → 85 |
| 구절이 시작될 때마다 19반음씩 튐 | 동기가 음역을 꽉 채우면 옥타브 보정 조건(`mn - 7 >= lo`)이 막혀 낱음만 `fit()` 으로 튐 | 보정을 조건부에서 “직전 음에 가장 가까운 옥타브 선택”으로 바꾸고, 후크 폭을 4도로 조임. 구절 안 도약 5건 → 1건 |
| 마스터 피크 1.21 (찌그러짐) | 리드를 키우면서 헤드룸이 없어짐 | 마스터 0.8 → 0.6, 컴프 비율 4 → 6. 피크 0.92 |
| 리드가 드럼·베이스에 묻힘 | K-pop 플럭 진폭이 작고 808 이 앞섬 | 플럭 진폭 0.2 → 0.32, 베이스 믹스 0.8 → 0.72. 리드 RMS 0.064 → 0.101 |
| **피아노롤에 음표가 한 개도 안 그려짐** | 전환 효과음 이벤트의 `p` 가 `"riser"` 같은 문자열인데 음역 계산에 섞여 들어가 `Math.min` 이 NaN → `py()` 전부 NaN | 음역 계산에서 `fx` 트랙 제외. 캔버스 픽셀을 세는 검사를 테스트에 넣어 재발을 막음 |
| 피아노롤 음표가 2×2.7px 라 사실상 안 보임 | 마디 폭 30px, 롤 높이 250px | 마디 40px·높이 300px, 최소 음표 3×3px |
| 피아노롤 코드 이름이 옆 마디와 겹침 | 마디 폭 30px에 긴 이름 | `measureText`로 재서 7음 생략 → 근음만 → 생략 순으로 줄인다 |
| 일렉피아노 트랙에 DC 오프셋(-0.035) | 인덱스 큰 FM 파형의 비대칭 | 마스터에 24Hz 하이패스 |

## 검증

헤드리스 크로미움(Playwright)에서 확인했다. 스크립트는 저장소에 넣지 않았다(테스트 전용 경로가 하드코딩돼 있어서).

- K-pop 예시 3개 + 로파이·시티팝·시네마틱·8비트·재즈·발라드·왈츠·앰비언트·동요·팝 + 빈 문장·무의미한
  문자열·영어·직접 지정(`A 단조`, `가단조`, `Bb`, `120bpm`, `2분`)·오탐 유도(`분위기`, `팝콘`, `블록`, `결혼`)
  까지 30여 문장 생성. 문장당 6~50ms, 잘못된 음표 0개, 정의되지 않은 드럼·효과음 0개
- 후크 문자열 길이 자동 검증(4/4는 32칸, 3/4는 24칸, 드롭은 한 마디) — 어긋남 0
- 멜로디: 당김음 비율 0.42~0.58(이전 0), 강박 음의 코드음 비율 0.94~1.00,
  12반음 넘는 도약은 구절 안 1건(섹션 경계 도약은 후렴 리프트라 의도한 것)
- K-pop 구조 확인: `intro:2 A:8 P:8 B:8 D:8 outro:2`, 프리코러스가 구절마다 65 → 69 → 72 → 74 → 81 로 상승
- 후렴 직전 컷: 프리코러스 마지막 두 박에 반주 이벤트 0개, 클랩 1개만 남음. 라이저·임팩트가 제 위치에
- 808 글라이드가 붙어 있는 54음에 적용
- 믹스(전곡 렌더): 피크 0.92(클리핑 없음), NaN 0, DC 0. 리드 RMS 0.101로 코드·패드보다 앞섬
- 피아노롤이 실제로 음표를 그리는지 캔버스 픽셀을 트랙 색깔별로 세어 확인(멜로디 4480 · 코드 5540 ·
  베이스 5095 · 패드 4386픽셀). 스크린샷만 믿었다가 아래 "효과음이 음역을 NaN 으로" 버그를 놓칠 뻔했다
- WAV 전곡 렌더 NaN 0, MIDI 는 7트랙(멜로디·코드·베이스·패드·아르페지오·드럼 + 메타)으로 나뉘고
  헤더·델타타임·노트온/오프 짝·EOT·템포(114bpm→526316µs)·박자·조표(D단조→♭1)가 모두 맞음
- 재생 → 일시정지 → 재개 → 진행바 탐색 → 처음으로, 390px 모바일 가로 스크롤 없음, 콘솔 오류 0

## 한계

- 목소리·가사는 없다. 리드 악기가 노래 선율을 맡고, 드롭의 “보컬 찹”은 포먼트 필터로 흉내 낸 신스다.
- 신경망이 아니라서 특정 곡·특정 아티스트의 색을 재현하지는 못한다. 장르의 공통 문법까지가 한계다.
- 합성 악기라 실제 녹음의 질감은 없다. MIDI로 가져가 DAW 악기로 바꾸는 쪽이 낫다.
- WAV 렌더는 `OfflineAudioContext.suspend` 로 0.5초씩 끊어 가며 음을 연결한다. 이걸 지원하지 않는 브라우저(파이어폭스 추정)는
  예전처럼 곡 전체를 한 번에 연결하므로 보통 길이 곡이 1분쯤 걸리고 진행률이 0%에서 100%로 바로 넘어간다.
- 실제 아이폰·안드로이드에서는 확인하지 못했다. 드럼 버퍼 공유, k-rate 필터 자동화, `latencyHint` 는 표준 API 지만 헤드리스 크로미움에서만 쟀다.
- 아직 남은 것: 프리코러스가 후렴보다 높게 끝나 후렴이 덜 뜨는 곡이 있고(측정 18/40), 절·프리코러스 멜로디의 4~5% 가 코드와 반음으로 부딪힌다.
  시티팝 스트링 패드는 rootless 일렉피아노와 반음으로 스치는 경우가 5.9% 있다.
- 공유 링크는 문장과 씨앗값으로 곡을 다시 만든다. 이번에 작곡 규칙이 바뀌어서, 예전에 복사한 링크는 다른 곡으로 열린다.
