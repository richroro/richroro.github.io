# 한 줄 작곡 — 글로 만드는 AI 음악

“요즘 K팝 걸그룹 타이틀곡”처럼 한 줄 적으면 장르·조성·템포·악기를 읽어 곡을 짓고,
브라우저가 바로 연주한다. 결과는 WAV(오프라인 렌더)와 MIDI(악기별 트랙)로 저장한다.

기본 목표는 2020년대 중반 K-pop 이다. 이지리스닝 댄스팝에 프로그레시브 하우스를 섞고,
후렴 직전에 반주를 끊었다가 내리꽂고, 후렴에서는 드럼을 앞세우고 멜로디는 단순하게 간다.

- 배포: https://richroro.github.io/ai-music/
- 소스: `index.html` 한 파일 (외부 스크립트·폰트·샘플 없음, 서버 없음)

## 솔직한 정의

학습된 신경망이 아니다. **음악 이론 규칙 + 확률(마르코프 체인·가중 추첨)** 로 작곡하는 엔진이고,
소리는 Web Audio 오실레이터·필터·노이즈로 그 자리에서 합성한다.
Suno·Udio처럼 목소리가 들어간 노래는 못 만든다. 대신 설치·가입·비용이 없고, 같은 문장·씨앗값이면
언제 어디서나 같은 곡이 나오며, MIDI로 가져가 DAW에서 손볼 수 있다.

Magenta.js(MusicRNN) 같은 브라우저용 신경망을 붙이는 것도 검토했지만, 체크포인트를 외부 CDN에서
내려받아야 하고(수십 MB) 그 CDN이 언제 사라질지 모른다. “한 파일, 의존성 0” 원칙을 지키는 쪽을 택했다.

## 되는 것

| 영역 | 내용 |
| --- | --- |
| 입력 | 한국어·영어 자유 문장. `120bpm`, `A 단조`, `다장조`, `2분` 같은 직접 지정도 읽는다 |
| 장르 | **K-pop**·발라드·팝·록·재즈·로파이·힙합·EDM·8비트·시네마틱·앰비언트·보사노바·왈츠(3/4)·시티팝·동요 (15종) |
| 해석 | 장르 점수, 밝기(valence)·에너지, 템포 가감, 선법(장·단·도리안·리디안·믹솔리디안·프리지안), 악기 지정, 5음 음계(국악), 독주(드럼·베이스 제거), 빗소리·파도 배경 |
| 작곡 | K-pop 은 인트로–절–**프리코러스**–후렴–**드롭**–아웃트로, 그 외는 인트로–절–후렴–브리지–아웃트로. 장르별 코드 진행 + 마르코프 생성, 성부 진행(voice leading), 2마디 통짜 후크의 이조·재작성·종지 변형, 장르별 베이스·반주·드럼 패턴, 필인·크래시, 스윙 |
| 소리 | 악기 28종(**K-pop 플럭·보컬 찹·글라이드 808**·피아노·일렉피아노·기타·스트링·패드·합창·슈퍼소우·8비트 펄스·벨·플루트·색소폰·베이스 8종…), 드럼 13종, 전환 효과음(라이저·임팩트), 컨볼루션 잔향·딜레이·사이드체인·로파이 비트크러시 |
| 재생 | 재생/일시정지/처음으로, 진행바·피아노롤 클릭으로 탐색, 스페이스바, 재생 위치 따라 피아노롤 자동 스크롤 |
| 손보기 | 템포·조성·선법·길이·스윙·드럼 유무·씨앗값. 바꾸면 즉시 다시 작곡 |
| 저장 | WAV 44.1kHz 16bit 스테레오(피크 정규화), MIDI SMF 1(템포·박자·조표, 악기별 트랙·GM 프로그램, 드럼은 10번 채널), 링크 복사(`#p=문장&s=씨앗…`) |

## 구조

`문장 → 설계도(P) → 악보(song) → 이벤트(초) → 소리` 네 단계가 한 방향으로만 흐른다.

```
interpret(prompt, seed, overrides)  →  P   { genre, bpm, root, mode, beats, swing, lead, chordInst, … }
compose(P)                          →  song { sections, chords, lead, chords_tr, bass, pad, arp, drums }   (박 단위)
renderEvents(song)                  →  song.events  [{ time(초), d, p(MIDI), v, tr, inst }]  (스윙·휴먼라이즈 적용)
buildGraph(ctx) + playEvent()       →  실시간 AudioContext 또는 OfflineAudioContext (같은 코드)
```

### 1. 문장 해석 (`LEX`, `interpret`)

- 78개 항목·500여 개 어간의 단서 사전. 각 항목은 어간 목록(`["잔잔","차분","calm",…]`)과 효과(장르 점수, 밝기, 에너지, 템포, 선법, 악기, 플래그)를 갖는다.
  조사·활용을 따로 처리하지 않고 **부분 문자열**로 맞춘다(“잔잔한”, “잔잔하게” 모두 “잔잔”에 걸린다).
- 장르는 점수 최댓값. 동점이면 씨앗값으로 고른다. 단서가 없으면 밝기·에너지로 후보군을 좁힌 뒤 씨앗값으로 고른다.
- 씨앗값은 `FNV-1a(문장) XOR (씨앗번호 × 황금비 상수)`. 같은 문장·번호면 같은 곡, “🎲 다른 버전”은 번호만 올린다.
- 화면의 “→ 「비」 비 오는 날 감성으로…” 목록이 실제로 걸린 단서다. 해석이 이상하면 이 목록에서 어느 단어 탓인지 바로 보인다.

### 2. 작곡 (`compose`)

- **구조**: 목표 길이(짧게 30초 / 보통 70초 / 길게 140초)를 템포·박자로 나눠 마디 수를 구하고, 8마디(짧으면 4마디) 섹션을
  `A B`, `A B A B`, `A B A B C B`… 순으로 채운다. 느린 발라드와 빠른 8비트가 같은 “보통”을 골라도 실제 길이가 비슷하다.
- **화성**: 절·후렴·브리지마다 진행을 한 번 정해 재사용한다(반복되는 절은 같은 코드). 70%는 장르별 진행표에서, 30%(브리지는 60%)는
  마르코프 전이표에서 뽑는다. 섹션 끝은 55% 확률로 딸림화음으로 바꾸고, 팝·발라드는 마지막 마디를 IV→V로 쪼개 종지를 만든다.
  단조의 V는 장르별 확률로 장3화음(화성단음계)이 된다. 7화음 확률도 장르별(재즈 100%, 록 0%).
- **보이싱**: 코드음마다 55~76 사이 옥타브 후보를 전부 조합해 “이전 보이싱에서 가장 적게 움직이는 것”을 고른다(성부 진행).
- **멜로디**: 이 부분을 한 번 갈아엎었다. 첫 판은 16분음표를 빈틈없이 채우는 리듬 셀을 썼는데,
  뽑아 보니 쉼표가 18~22%뿐이고 당김음이 **0**이었다 — 쉬지 않고 흥얼대는, 외울 수 없는 선율.
  지금은 **2마디 통짜 후크를 문자열로 적는다**(`x`=음 시작, `-`=이어짐, `.`=쉼표):

  ```
  "x-x-x---x-x-x---x-x-x-------...."   ← K-pop 후렴
  "..x-x-x---......x-x-x-----------"   ← 당김음으로 들어가는 절
  ```

  눈으로 보고 고칠 수 있어서 "구절 끝을 비운다", "센박을 피해 들어간다" 같은 규칙을 지키기 쉽다.
  음높이는 강박=코드음, 약박=순차 진행을 선호하는 가중 추첨으로 붙이되 **5음 음계에 무게를 싣고**
  (팝 계열은 5음 밖 가중치 0.32~0.5), 후크 계열은 **같은 음을 두 번 치는 걸 오히려 권한다**.
  8마디는 `동기 → 이조/재작성 → 동기 → 종지 변형` 네 구절이고, 동기 폭은 4도 안으로 묶는다.
- **섹션 대비**: 절은 음역 아래쪽(0.30 지점)에서 성기게, 프리코러스는 구절마다 한 도씩 올라가며
  딸림화음으로 열어 두고, 후렴은 음역 위쪽(0.70)에서 한 단 촘촘하게. 드롭은 한 마디 리프를 통째로 반복한다.
- **후렴 직전 컷**: 프리코러스 마지막 두 박에서 반주·드럼을 전부 지우고 클랩 한 방만 남긴 뒤,
  2마디 라이저로 끌어올려 후렴 첫 박에 임팩트를 얹는다.
- **반주**: 장르별 스타일 키 하나로 분기한다 — 아르페지오(발라드), 블록(팝), 컴핑(재즈, 찰스턴 리듬 추첨), 스트럼, 스탭(EDM 뒷박),
  16분 아르페지오(8비트), 움파(왈츠), 서스테인(시네마틱·앰비언트), 파워코드 8분(록), 로파이 두 번 치기(9음 추가 35%).
- **베이스**: 루트·5도·옥타브·3도·접근음 역할 표기로 패턴을 적고, 워킹 베이스는 `루트 → 3/5도 → 경과음 → 다음 근음의 반음 접근`.
- **드럼**: 16스텝 확률표(3박자는 12스텝). 1.0은 항상, 그 아래는 섹션 강도(절 .85, 후렴 1, 브리지 .75)를 곱해 추첨.
  섹션 마지막 마디는 80% 확률로 필인(스네어→탐, EDM 스네어 롤, 시네마틱 팀파니 롤), 후렴 첫 박 크래시, 아웃트로 첫 박에 마지막 한 방.

### 3. 소리 (`INST`, `DRUM`, `buildGraph`)

- 악기 함수 하나가 음 하나다: `(g, out, f, t, d, v)`. 오실레이터 1~5개 + 게인 ADSR + 필터. 감쇠는 `setTargetAtTime`(지수)로,
  릴리스 뒤 실제로 조용해지는 시각에 `stop()` 해 노드가 남지 않는다.
- 피아노는 삼각파+2배음 사인+약한 톱니를 저역 필터로 뭉치고 **음이 높을수록 빨리 사라지게** 했다. 일렉피아노는 2-오퍼레이터 FM(비율 1:1과 1:14).
  8비트 펄스는 `PeriodicWave`로 25% 듀티를 만든다. 벨·실로폰은 비정수 배음(2.756×, 5.4×)의 사인.
- 그래프: `트랙 게인 → (사이드체인) → 마스터 → [로파이: LPF 3.4k + 계단형 WaveShaper] → DC 차단 HPF → 컴프레서 → 출력`.
  잔향은 씨앗값으로 만든 지수 감쇠 노이즈 임펄스를 `ConvolverNode`에, 딜레이는 점8분 피드백. EDM은 킥마다 패드·아르페지오 게인을 0.22로 눌렀다 0.3초에 되돌린다.
- 실시간 재생은 120ms마다 0.5초 앞까지만 스케줄하는 lookahead 방식, WAV는 같은 `playEvent`를 `OfflineAudioContext`에 전부 스케줄한다.
  일시정지는 `ctx.suspend()`, 탐색은 그 시각 이후 이벤트만 다시 스케줄한다(그 전에 시작된 긴 음은 빠진다).

## 만들면서 걸린 것

| 증상 | 원인 | 대응 |
| --- | --- | --- |
| 80초 곡 WAV 렌더에 118초, 렌더 결과 RMS가 피크와 거의 같음 | 공용 `finish()`가 `start()`를 시각 없이 불러 **모든 오실레이터가 0초부터** 돌았다 | `start(t)`. 렌더 14초로, 크레스트 팩터 1.2 → 3.5~5.8 |
| 비브라토 악기에서 `cannot call start more than once` | LFO를 `vibrato()`에서 시작하고 `finish()`가 또 시작 | 시작은 한 곳에서만 |
| 62bpm 로파이의 “보통”이 2분 27초 | 구조를 마디 수로 고정해 템포가 느릴수록 길어짐 | 목표 초를 템포·박자로 나눠 섹션 수를 정한다 |
| **멜로디가 흥얼거려지지 않음** | 16분음표를 빈틈없이 채우는 리듬 셀 — 쉼표 18~22%, **당김음 0** | 2마디 통짜 후크 문자열로 교체. 쉼표가 생기고 당김음 0 → 0.42~0.58 |
| **나(B)장조 곡이 다장조보다 한 옥타브 높이 뜸** | `degToMidi` 가 `60 + P.root` 로 조성을 그대로 더해서, 같은 음역 설정이 키마다 딴 음역 | 조성을 C 주위로 접는다(`root > 6 → root - 12`). 최고음 97 → 85 |
| 구절이 시작될 때마다 19반음씩 튐 | 동기가 음역을 꽉 채우면 옥타브 보정 조건(`mn - 7 >= lo`)이 막혀 낱음만 `fit()` 으로 튐 | 보정을 조건부에서 “직전 음에 가장 가까운 옥타브 선택”으로 바꾸고, 후크 폭을 4도로 조임. 구절 안 도약 5건 → 1건 |
| 마스터 피크 1.21 (찌그러짐) | 리드를 키우면서 헤드룸이 없어짐 | 마스터 0.8 → 0.6, 컴프 비율 4 → 6. 피크 0.92 |
| 리드가 드럼·베이스에 묻힘 | K-pop 플럭 진폭이 작고 808 이 앞섬 | 플럭 진폭 0.2 → 0.32, 베이스 믹스 0.8 → 0.72. 리드 RMS 0.064 → 0.101 |
| **피아노롤에 음표가 한 개도 안 그려짐** | 전환 효과음 이벤트의 `p` 가 `"riser"` 같은 문자열인데 음역 계산에 섞여 들어가 `Math.min` 이 NaN → `py()` 전부 NaN | 음역 계산에서 `fx` 트랙 제외. 캔버스 픽셀을 세는 검사를 테스트에 넣어 재발을 막음 |
| 피아노롤 음표가 2×2.7px 라 사실상 안 보임 | 마디 폭 30px, 롤 높이 250px | 마디 40px·높이 300px, 최소 음표 3×3px |
| 피아노롤 코드 이름이 옆 마디와 겹침 | 마디 폭 30px에 긴 이름 | `measureText`로 재서 7음 생략 → 근음만 → 생략 순으로 줄인다 |
| 일렉피아노 트랙에 DC 오프셋(-0.035) | 인덱스 큰 FM 파형의 비대칭 | 마스터에 24Hz 하이패스 |

## 검증

헤드리스 크로미움(Playwright)에서 확인했다. 스크립트는 저장소에 넣지 않았다(테스트 전용 경로가 하드코딩돼 있어서).

- K-pop 예시 3개 + 로파이·시티팝·시네마틱·8비트·재즈·발라드·왈츠·앰비언트·동요·팝 + 빈 문장·무의미한
  문자열·영어·직접 지정(`A 단조`, `가단조`, `Bb`, `120bpm`, `2분`)·오탐 유도(`분위기`, `팝콘`, `블록`, `결혼`)
  까지 30여 문장 생성. 문장당 6~50ms, 잘못된 음표 0개, 정의되지 않은 드럼·효과음 0개
- 후크 문자열 길이 자동 검증(4/4는 32칸, 3/4는 24칸, 드롭은 한 마디) — 어긋남 0
- 멜로디: 당김음 비율 0.42~0.58(이전 0), 강박 음의 코드음 비율 0.94~1.00,
  12반음 넘는 도약은 구절 안 1건(섹션 경계 도약은 후렴 리프트라 의도한 것)
- K-pop 구조 확인: `intro:2 A:8 P:8 B:8 D:8 outro:2`, 프리코러스가 구절마다 65 → 69 → 72 → 74 → 81 로 상승
- 후렴 직전 컷: 프리코러스 마지막 두 박에 반주 이벤트 0개, 클랩 1개만 남음. 라이저·임팩트가 제 위치에
- 808 글라이드가 붙어 있는 54음에 적용
- 믹스(전곡 렌더): 피크 0.92(클리핑 없음), NaN 0, DC 0. 리드 RMS 0.101로 코드·패드보다 앞섬
- 피아노롤이 실제로 음표를 그리는지 캔버스 픽셀을 트랙 색깔별로 세어 확인(멜로디 4480 · 코드 5540 ·
  베이스 5095 · 패드 4386픽셀). 스크린샷만 믿었다가 아래 "효과음이 음역을 NaN 으로" 버그를 놓칠 뻔했다
- WAV 전곡 렌더 NaN 0, MIDI 는 7트랙(멜로디·코드·베이스·패드·아르페지오·드럼 + 메타)으로 나뉘고
  헤더·델타타임·노트온/오프 짝·EOT·템포(114bpm→526316µs)·박자·조표(D단조→♭1)가 모두 맞음
- 재생 → 일시정지 → 재개 → 진행바 탐색 → 처음으로, 390px 모바일 가로 스크롤 없음, 콘솔 오류 0

## 한계

- 목소리·가사는 없다. 리드 악기가 노래 선율을 맡고, 드롭의 “보컬 찹”은 포먼트 필터로 흉내 낸 신스다.
- 신경망이 아니라서 특정 곡·특정 아티스트의 색을 재현하지는 못한다. 장르의 공통 문법까지가 한계다.
- 합성 악기라 실제 녹음의 질감은 없다. MIDI로 가져가 DAW 악기로 바꾸는 쪽이 낫다.
- WAV 렌더는 음 하나마다 오실레이터를 새로 만들기 때문에 촘촘한 곡일수록 오래 걸린다.
  헤드리스 크로미움에서 K-pop 77초 곡이 66초(악기를 가볍게 하기 전 80초). 실제 브라우저는 이보다 빠르다.
  버튼에 경과 시간을 띄워 멈춘 게 아니라는 걸 보이게 했다.
- 단서 사전에 없는 말은 씨앗값으로 처리된다. 화면의 “이렇게 읽었어요” 목록이 비어 있으면 그 경우다.
