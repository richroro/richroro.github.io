# 한 손 농구

공을 스와이프해서 던지는 세로형 농구 게임. 바람·가로막·동전이 하나씩 붙는
스테이지 12개, 아이템 5종과 상점, 전부 깨면 열리는 45초 타임어택.

- 배포: https://richroro.github.io/games/basketball/
- 소스: `index.html` 한 파일 (외부 스크립트·에셋·빌드 없음)

## 조작과 점수

화면을 누른 채 던질 방향으로 끌면 예상 궤적이 점으로 보이고, 손을 떼면 그대로 날아간다.
길게 끌수록 세게 나간다. 18단위보다 짧게 끌면 던지지 않는다(주머니 속 오터치 방지).

조준은 **처음 누른 손가락 하나**로만 한다. 조준 중에 다른 손가락이 닿아도 기준점이 옮겨지거나
대신 던져지지 않는다. 마우스는 왼쪽 버튼만 받고(왼쪽을 누른 채 오른쪽도 눌렀다면 왼쪽을 떼는 순간 던진다),
코트 위에서는 우클릭 메뉴를 막았다.
공이 돌아오는 중에 미리 눌러 두면, 공이 준비되는 순간 그 손가락이 그대로 조준이 된다(공 둘레에
파란 고리가 한 번 퍼진다). 기준점은 누른 자리가 아니라 **그 순간 손가락이 있는 자리**라서,
기다리며 움직인 만큼 힘이 미리 실리지 않는다.

| 상황 | 점수 |
| --- | --- |
| 림이나 백보드를 맞고 들어감 | 2점 |
| 아무것도 안 닿고 들어감(스윔) | 3점 |
| 2~3연속 / 4~5연속 / 6연속 이상 | +1 / +2 / +3점 |
| 동전 하나 | 2점 (+지갑 1코인) |
| 불공으로 넣음 / 황금 공으로 넣음 | 2배 / 3배 (겹치면 6배) |

연속 보너스는 2연속부터 붙고(위 표), 3연속부터는 공에 불까지 붙는다.
2연속부터 코트 위 가운데에 주황 배지 `🔥 N연속`이 뜨고, 연속이 바뀔 때마다 한 번 통 튄다('동작 줄이기'를 켜면 안 튄다).
배지는 공보다 먼저(밑에) 그려서, 높이 띄운 공이 배지 앞을 지나가도 공이 가려지지 않는다. 배지는 그림이라 화면 읽기용으로
코트 캔버스의 이름에도 연속을 붙인다(`농구 코트 · 3연속`). 림을 맞고 들어간 연속 골은 득점 글자 둘째 줄에 배지와 같은 어순(`2연속!`)으로 적힌다.
화면 위쪽의 목표·점수 칩은 보이는 숫자가 바뀐 칩만 한 번 튄다(골 3/3을 채운 뒤 더 넣으면 점수 칩만 튄다).
3스테이지부터는 공 8개 중 하나꼴(12%)로 **황금 공**이 나온다. 넣으면 점수 3배.

## 아이템

코트 아래 막대에서 눌러 켜 두면 **다음 슛 한 번**에 걸리고, 던지는 순간 하나 쓴다.
다시 누르면 꺼지고 개수는 그대로다. 공 +2 만 누르는 즉시 쓴다.

| 아이템 | 값 | 효과 |
| --- | --- | --- |
| 🧲 자석 | 20 | 림 위쪽 반원(반지름 92) 안에 들어온 공을 가운데로 끌어당긴다 |
| 🎯 조준선 | 12 | 궤적 점선을 14개에서 60개로, 끝까지 보여 준다 |
| ❄️ 얼음 | 20 | 켜는 순간부터 골대·가로막·바람이 멈춘다 |
| ☄️ 불공 | 25 | 가로막을 뚫고 지나가고, 넣으면 점수 2배 |
| ➕ 공 +2 | 15 | 공 2개를 더 받는다 (타임어택에서는 못 씀) |

**얻는 법** — 처음 온 사람은 자석·조준선을 하나씩 받고 시작한다. 코트에 떠 있는
아이템 방울을 공으로 맞히면 하나 들어온다. 방울은 보통 포물선보다 72만큼 위에
띄워 뒀다. 얻으려면 높이 띄워야 해서 "골을 노릴지, 방울을 노릴지" 한 번 고르게 된다.
나머지는 코인으로 상점에서 산다.

**코인** — 동전 하나에 1, 클리어하면 별 하나에 3, 타임어택은 10점에 1.
별 3개를 **처음** 받은 스테이지는 아이템도 하나 선물한다. 매번 주면 1스테이지만
돌려서 아이템을 무한히 모을 수 있어서 처음 한 번으로 막았다.

**자석이 얼마나 센가** — 넣을 수 있는 슛을 찾아 놓고, 사람 손 떨림만큼 속도에 오차를
섞어 200번씩 던져 비교했다(골대 거리 4가지 × 오차 3단계).

| 속도 오차 | 자석 없음 | 자석 |
| --- | --- | --- |
| ±1.5% (잘 던짐) | 57~67% | 92~98% |
| ±2.5% (보통) | 52~59% | 82~85% |
| ±4.0% (막 던짐) | 44~55% | 68~80% |

평균 +28%p. 잘 던진 슛은 거의 다 살리지만, 막 던진 슛은 넷 중 하나가 여전히 빗나간다.
20코인짜리 소모품으로 이 정도가 적당하다고 봤다. 처음 측정할 땐 "중심에서 30 안쪽으로
빗나간 슛"을 따로 세려 했는데 그런 슛이 하나도 없었다. 림 끝(반지름 31)과 공(17)이
있어서 그 안쪽을 지나는 공은 반드시 림에 맞고 튕겨 나가, 50 바깥에서 기록되기 때문이다.

## 스테이지

목표를 **전부** 채우면 클리어, 공을 다 쓰면 실패. 앞 스테이지를 깨야 다음이 열린다.

| # | 이름 | 목표 | 공 | 새로 나오는 것 |
| --- | --- | --- | --- | --- |
| 1 | 첫 슛 | 3골 | 8 | — |
| 2 | 거리 감각 | 4골 | 9 | 던지는 자리가 매번 달라짐 |
| 3 | 움직이는 골대 | 4골 | 10 | 골대 좌우 이동 |
| 4 | 바람 | 4골 | 10 | 바람(오른쪽) |
| 5 | 동전 줍기 | 3골 + 동전 4 | 10 | 동전 8개 |
| 6 | 가로막 | 4골 | 9 | 움직이는 막대 1개 |
| 7 | 좁은 림 | 4골 | 10 | 림 반지름 31 → 27 |
| 8 | 출렁이는 골대 | 4골 | 10 | 골대 위아래 흔들림 |
| 9 | 맞바람 | 5골 | 13 | 바람(왼쪽, 약 1.7배) |
| 10 | 이중 가로막 | 5골 | 11 | 막대 2개 + 흔들림 |
| 11 | 스윔만 인정 | 스윔 3 | 14 | 림에 닿으면 무효 |
| 12 | 결승 | 5골 + 동전 3 | 14 | 전부 + 90초 제한 |

별은 **실제로 던진 공이 몇 개였는지**로 준다. 주어진 공의 절반 이하로 깨면 별 3개,
4분의 3 이하면 2개, 그 밖에 클리어하면 1개. 처음엔 "남은 공 3개 이상"이라는 절대
개수로 뒀는데, 그러면 공을 14개 주는 뒤쪽 스테이지가 자동으로 별 3개가 돼서 비율로
바꿨다. 남은 공이 아니라 던진 공으로 세는 건 공 +2 로 별을 살 수 없게 하려는 것이다.

**제한 시간(결승·타임어택)** — 0초가 되는 순간 목표부터 본다. 이미 다 채웠으면 골 뒤에 다음
공을 기다리는 0.5초 사이였어도 클리어다. 공이 날아가는 중이면 버저가 울리고 시간 표시가
`버저!`로 바뀌며, 그 공은 끝까지 지켜본다. 들어가면 `버저비터!`, 안 들어가면 공이 떨어진 뒤
"시간이 끝났어요". 공을 들고 있었다면 예전처럼 바로 끝난다. 타임어택은 버저비터로 넣어도
시간이 늘지 않는다(0초에서 +2초가 되면 끝난 경기가 다시 시작된다). 0초 뒤로는 공이 다시 안 오니
아이템을 누르면 "시간이 끝나서 더 못 써요".

바람은 궤적 점선에 이미 반영돼 있다. 안 그러면 속이는 셈이 된다.
반대로 림·백보드·막대 충돌은 일부러 예측하지 않는다. 점선은 14개에서 끊기므로
마지막 한 뼘은 눈으로 재야 한다.

## 난이도 측정

궤적을 직접 예측해 조준하는 봇으로 스테이지마다 5판씩 돌렸다. 봇은 골대의 좌우 이동까지는
예측하지만 위아래 흔들림은 예측하지 않고, 빗나가면 사람처럼 조금 고쳐 던진다.

| 스테이지 | 클리어 | 평균 사용 공 | 별 평균 |
| --- | --- | --- | --- |
| 1 첫 슛 | 4~5/5 | 4.6/8 | 2.8 |
| 2 거리 감각 | 5/5 | 5.8/9 | 2.4 |
| 3 움직이는 골대 | 5/5 | 6.2/10 | 2.4 |
| 4 바람 | 5/5 | 6.6/10 | 2.0 |
| 5 동전 줍기 | 5/5 | 4.0/10 | 2.8 |
| 6 가로막 | 5/5 | 6.4/9 | 2.2 |
| 7 좁은 림 | 4~5/5 | 6.0/10 | 2.8 |
| 8 출렁이는 골대 | 5/5 | 6.8/10 | 2.0 |
| 9 맞바람 | 5/5 | 7.2/13 | 2.6 |
| 10 이중 가로막 | 5/5 | 6.4/11 | 2.6 |
| 11 스윔만 인정 | 5/5 | 4.8/14 | 3.0 |
| 12 결승 | 5/5 | 6.2/14 | 2.8 |

5판 중 4판만 깨는 스테이지가 매번 자리를 바꿔 가며 한두 개씩 나온다. 특정 판이 어려워서가
아니라 봇이 후보 중 하나를 골라 던지는 데서 오는 흔들림이다. 전 스테이지가 주어진 공의
절반 안팎만 쓰고 끝나므로 여유는 충분하다. 위 표는 씨앗 없이 여러 번 돌린 범위다. 회귀 검사용 표는
판·회차마다 씨앗을 고정해 돌린다(돌릴 때마다 WARN 이 오가면 코드가 바뀐 건지 운인지 모른다).
그 씨앗 한 벌에선 1스테이지가 3/5로 나오는데, 이 패키지 전 코드도 12판 전부 숫자까지 똑같다.

## 구현 메모

물리는 고정 스텝(1/60초) 적분이다. 스테이지를 붙이면서 실제로 걸렸던 것들:

| 증상 | 원인 | 대응 |
| --- | --- | --- |
| **긴 폰에서 1스테이지가 제일 어려움** | 코트 세로를 화면 비율대로 늘려서, 세로가 긴 기기일수록 골대가 멀어졌다. 390×844에서는 필요한 힘이 상한의 86%까지 올라가 거의 최대 힘으로만 들어갔다 | 코트를 **420×680 고정**으로 두고 비율을 지킨 채 화면 가운데에 놓는다. 어느 기기에서나 같은 난이도 |
| 넣을 수 있는 슛이 거의 수직 로브뿐 | 골대와 던지는 자리가 가로로 210밖에 안 떨어졌는데 높이 차이는 390이었다 | 골대를 오른쪽(205~366)으로, 던지는 자리를 왼쪽(50~130)으로 벌리고 림을 0.32→0.42 높이로 내렸다 |
| **동전 스테이지를 재도전하면 영영 못 깸** | 화면 회전 대비로 넣어 둔 "다시 놓을 때 먹은 동전 유지" 코드가 `beginStage` 에도 걸려서, 두 번째 판부터 동전이 이미 먹힌 상태로 시작됐다 | 코트가 고정이라 다시 놓을 일 자체가 없어졌으므로 유지 코드를 들어냈다 |
| 동전을 주우려면 골을 포기해야 함 | 동전을 코트에 부채꼴로 뿌려서 골 넣는 길과 따로 놀았다 | 던지는 자리에서 골대까지의 포물선 위에 올리고, 길의 80% 지점에서 끊어 림과 겹치지 않게 했다 |
| 뒤쪽 스테이지가 자동으로 별 3개 | 별 기준이 "남은 공 3개 이상"이라는 절대 개수였다 | 사용한 공의 **비율**로 바꿨다 |
| 남은 시간이 `45초`에서 한참 안 바뀜 | 1/60씩 빼다 보면 44가 `44.0000000001`로 남아 `Math.ceil`이 45를 돌려줌 | `Math.ceil(t - 1e-6)` |
| 얼음을 공짜로 쓸 수 있음 | 공이 날아가는 중에 얼음을 켜면 **지금 공**의 골대가 멈추고, 착지 전에 끄면 하나도 안 쓴 채 끝났다 | 공이 날아가거나 돌아오는 중에는 아이템을 켤 수 없게 막았다 |
| "아이템 써 보세요" 안내가 다른 알림을 덮음 | 첫 안내를 스테이지 시작 0.6초 뒤에 띄우는데, 그사이 "공이 돌아오면 쓸 수 있어요" 같은 알림이 떠 있으면 덮어썼다 | 다른 알림이 떠 있으면 건너뛰고, 실제로 보여 준 뒤에만 "봤음"으로 저장 |
| **결승에서 목표를 다 채웠는데 시간이 끝났어요** | 시간 판정이 골 뒤 0.5초 기다렸다 도는 `afterShot`(목표 판정)보다 먼저 돌아서, 마지막 골과 0초가 0.5초 안에 겹치면 실패였다 | 0초에 목표를 먼저 본다. 날아가는 공은 버저비터로 끝까지 보고 결론은 `afterShot`이 낸다. 타임어택은 버저비터로 시간이 늘지 않는다 |
| **클리어·실패 화면에서 목록을 열었다 닫으면 빈 코트에 갇힘** | 목록의 닫기가 "게임 중 일시정지"와 "시작 전" 두 경우만 알았다. 클리어·실패·타임어택 결과에서 열면 아무 시트도 안 뜨고 코트는 멈춰 있었다 | 닫을 때마다 지금 상태(클리어·실패·결과·일시정지·시작 전)로 돌아갈 시트를 되짚는다. 따로 기억하는 변수가 없어서 목록 → 상점 → 닫기 → 목록 왕복에도 그대로 맞는다 |
| 안내만 보고 돌아와 "다시"를 누르면 다른 판이 시작 | 시작 안내를 여는 것만으로 `stage.idx`를 바꿨다. 1스테이지를 깨고 "다음 스테이지" 안내만 본 뒤 돌아와 "다시"를 누르면 2스테이지가 시작됐다 | 안내는 자기 번호(`introIdx`)만 쓰고, 연 자리(목록·클리어·시작)로 돌아가는 **뒤로** 버튼을 달았다. 다시하기 세 곳은 들고 있는 정의로 재시작(`restartStage`) |
| **예외 한 번에 화면이 영영 굳음** | 다음 프레임 예약(`requestAnimationFrame`)이 프레임 맨 끝에 있어서, 그리기나 물리에서 한 번이라도 예외가 나면 루프가 끊겼다 | 예약을 맨 앞으로 옮기고 물리·그리기를 따로 `try`로 감쌌다. 날던 공은 믿을 수 없으니 그 슛을 끝내고(던진 공은 쓴 것으로) "공이 사라졌어요. 다음 공으로 이어 가요"와 함께 다음 공을 준다. 말없이 없애면 화면이 깨진 줄 안다. 처음엔 "다음 공을 드려요"라고 썼는데, 공을 돌려주는 NaN 환불("돌려드렸어요")처럼 들려서 있는 그대로만 말한다. 10초 안에 6번째 예외가 나면 매 프레임 터지는 것이니 "문제가 생겼어요" 시트(처음으로·다시하기) |
| **오류 시트가 끝없이 다시 뜸** | 그리기가 매 프레임 터지면(GPU·브라우저 버그 등) 다시하기를 눌러도, 처음으로를 눌러도 0.1초 뒤 같은 시트가 떴다. 시작 화면에선 물리가 안 도니 거기서 시트가 떴다면 늘 이 경우인데, 버튼은 회색 "처음으로" 하나에 안내는 "처음으로 돌아가서 다시 시작해 주세요"였다. 손으로 새로고침하기 전엔 못 나갔다 | 시작 화면에서 났거나, 다시하기·처음으로를 누르고 온 바로 그 판(시작 화면)에서 또 열리면 다시하기 대신 **새로고침**을 주 버튼으로 권한다("새로고침하면 보통 다시 할 수 있어요", 저장이 되는 브라우저면 "깬 스테이지와 별은 그대로 남아요"). 처음으로는 보조 버튼으로 남긴다. 새로 고른 판에서 난 오류는 처음처럼 다시하기. 시트가 떠 있는 동안 계속 터져도 시트를 다시 만들지 않는다 |
| NaN 공이 동전을 전부 먹음 | 실제로 본 적은 없고 일부러 속도에 NaN을 넣어 봤다. NaN과의 비교는 전부 거짓이라 충돌·줍기·득점 판정을 모두 "통과"해서, 5스테이지에서 동전 8개를 한 스텝에 다 먹고 16점이 올랐다 | 충돌·줍기 전에 좌표·속도가 유한한지 본다. 아직 결과가 안 난 슛이면 공을 돌려주고("공이 이상하게 튀어서 하나 돌려드렸어요") 던질 때 쓴 아이템도 돌려서 켜 둔 채로 둔다(처음엔 공만 돌려줘서 아이템은 사라졌다). 황금 공이었으면 황금 공으로 돌려준다(다음 공을 새로 뽑아서, 돌려준 황금 공이 88%는 보통 공이 됐다). 이미 들어갔거나 떨어진 슛이면 기록은 두고 공만 치운다(여기서 다음 공을 바로 주면 목표 판정을 건너뛴다) |
| **공이 영영 안 내려옴** | 골대가 안 움직이는 1·2스테이지에서 백보드 윗면(폭 9)에 공을 올려 두면 끝없이 통통 튀며 다음 공이 오지 않았다. 무작위 슛도 **얼음을 켜면** 예전 코드를 붙잡았다. 막대·골대를 아무 위치에 두고 얼음을 켠 무작위 4,000샷씩에서, 12스테이지 1개는 오른쪽으로 치우쳐 선 골대의 백보드 모서리와 오른쪽 벽 사이(28, 공 지름 34보다 좁다)에 끼어 5,000스텝(83초)이 지나도 그대로였다. 10스테이지 1개는 얼음으로 선 위 막대에 얹혀 속도 0.1 안팎으로 22초를 기어가다 막대 끝에서 떨어졌다. 조금만 덜 굴렀으면 막대 위에 그대로 섰다 | 워치독: 8초 넘게 날거나, 공중에서 1.5초 동안 **거의 멈춘 채(속도 0.6 미만) 제자리에** 있으면 빗나감으로 끝낸다. 처음엔 가로막 위에 얹힌 공도 멈춘 것으로 셌더니, 10스테이지 아래 막대 위에 1.5초 넘게 얹혀 있던 정상 슛이 무작위 36,000샷 중 5개 끊겼다. 막대가 움직이면 공은 알아서 떨어지고 결승에선 떨어지다 아래 동전을 먹을 수도 있어서, 움직이는 막대에 닿아 있는 동안은 세지 않는다(얼음으로 선 막대는 센다). 속도만 볼 때는 **천천히 굴러가는 공**도 끊었다. 1스테이지에서 백보드 윗면에 떨어진 공이 스텝당 0.07 안팎으로 왼쪽 끝까지 굴러 림으로 떨어져 들어가는 슛이 1.5초가 먼저 차서 빗나감이 됐다(사람이 던질 만한 림 근처 슛 40,000개 중 1개, 그 슛 주변 각도·세기를 0.02 간격으로 훑은 15,251샷에서 2개 더. 예전 코드로는 셋 다 골). 그래서 세기 시작한 자리에서 2 넘게 벗어나면 처음부터 다시 센다. 제자리에서 떨기만 하는 공은 그대로 1.5초에 끊기고, 얼음으로 선 막대 위를 기어가는 공은 막대 끝에서 떨어지거나 8초 상한에서 끝난다(아래 무작위 30,000샷 중 얼음으로 선 10스테이지 막대 위에서 예전 코드로 50초 넘게 안 끝나던 1개는 181스텝 대신 480스텝에서 끊겼다. 늦어졌지만 결과는 같다). 8초 상한은 넉넉하지 않다. 처음엔 판을 시작하자마자 던져서(막대가 늘 출발 위치) 최장 비행 312스텝으로 쟀는데, 사람이 던질 때처럼 슛 전에 0~600스텝을 돌려 막대를 아무 위치에 두면 10스테이지에서 위 막대를 타다 아래 막대로 떨어지는 공이 예전 코드로 약 508스텝(8.5초)까지 난다. 그래서 아이템 없는 10스테이지 슛 55,000개 중 3개가 8초에서 끊겼다. 지금 규칙으로 예전 코드(워치독 없음)와 같은 난수로 나란히 돌리면 **결과가 바뀐 슛은 0**이다: 아이템을 켠 무작위 30,000샷, 가로막 판(6·10·12) 무작위 10,000샷, 1·2스테이지 림 근처 40,000샷, 위 15,251샷. 일부러 공을 멈춘 백보드 윗면·막대 위 같은 자리에 올려 두면 다르다. 그런 배치 20,000번 중 55번은 결과가 바뀌었다(속도만 볼 때는 132번. 예전 코드에선 2.5~9초 뒤 굴러떨어져 들어가기도 했다). 어느 경우든 공은 509스텝(8초 + 결과 뒤 0.5초) 안에 끝났다 |
| **두 번째 손가락이 조준을 가로챔** | 조준 중에 다른 손가락이 닿으면 기준점을 그 자리로 옮겼고, 어느 손가락을 떼든 던졌다. 화면 구석을 스친 손가락 하나에 공이 반대로 날아갔다(실측 vx −16.6) | 조준을 시작한 `pointerId` 하나만 받는다. 마우스는 왼쪽 버튼만, 시스템이 손가락을 가져가면(`pointercancel`) 던지지 않고 푼다 |
| 마우스 오른쪽을 뗄 때 던져짐 | 왼쪽으로 조준하다 오른쪽도 누르면, 왼쪽을 떼도 브라우저는 `pointerup`이 아니라 `pointermove`(button 0, buttons 2)를 보낸다. `pointerup`은 마지막 버튼을 뗄 때 한 번이라, 오른쪽을 마저 뗄 때 던져졌다 | 조준하던 마우스의 `pointermove`에서 왼쪽 버튼이 떨어졌으면(button 0, buttons 에 왼쪽 없음) 그때를 떼기로 본다. 뒤따르는 `pointerup`은 조준이 이미 풀려 무시된다. 공이 오는 중에 미리 누른 왼쪽도 같은 식으로 떼면 조준으로 이어 주지 않는다 |
| 떼기를 놓치면 다음 조준이 먹힘 | 위 규칙을 넣고 나서 생겼다. 조준하던 포인터의 떼기(`pointerup`)가 안 오면(창 밖에서 마우스를 뗌 등) 조준 중으로 남아, 같은 포인터의 다음 누름까지 "두 번째 손가락"으로 보고 무시했다. 그걸 떼면 예전 기준점에서 던져 아래로 날아갔다(실측 vx −18.3, vy +22.5) | 조준하던 그 `pointerId`가 다시 누르면 앞의 떼기를 놓친 것으로 보고 새 기준점으로 받는다. 다른 포인터는 그대로 무시 |
| 공이 돌아오기 직전에 누르면 무시됨 | 공이 준비 안 됐을 때 누른 건 그냥 버렸다. 손을 뗐다 다시 눌러야 했다 | 기억해 뒀다가 공이 준비되는 순간 조준으로 바꾼다. 기준점은 그 순간의 손가락 위치. 그 순간 공 둘레에 파란 고리가 0.4초 퍼진다(처음 0.25초·두께 2·반투명 0.5는 폰에서 거의 안 보였다) |
| **타임어택이 끝나면 코인이 2~4배로 들어옴** | 늦은 프레임(30fps 절전, GC 멈춤)은 고정 스텝을 몰아서 도는데, 판이 끝난 뒤에도 그 프레임의 남은 스텝을 돌렸다. 공을 든 채 0초가 되면 남은 스텝마다 결론이 다시 나서, 점수 12(코인 +1)에 33ms 프레임이면 +2, 67ms 프레임이면 +4가 지갑에 들어갔다(결과 시트엔 +1). 결승에선 실패음이 겹쳤다. 이 수정 전부터 있던 문제다 | 판이 끝나면 그 프레임의 남은 스텝을 돌리지 않는다. 시간 판정(0초 클리어·버저·결론) 전체도 판이 진행 중일 때만 한다. 결론만 막았을 땐 시간을 조금 남기고 깬 결승에서 스텝이 더 돌면(테스트의 `H.step`) 0초에 클리어가 또 나서 코인·별 보상이 두 번 들어왔다 |
| 조준 중에 목록을 열면 조준이 남음 | 일시정지·나가기는 조준을 놓는데 목록(☰)만 안 놓았다. 다른 손가락으로 ☰을 누르면 목록 밑에 조준점이 남고, 목록을 닫은 뒤 첫 손가락을 떼면 예전 조준대로 날아갔다 | 목록·상점을 열 때도 조준을 놓는다 |
| 상점에 다녀오면 결과 시트가 예전 지갑을 보여 줌 | 실패 시트의 권유 줄("상점에서 아이템을 살 수 있어요 · 지갑 🪙 30")과 클리어 시트의 지갑은 판이 끝날 때 한 번만 썼다. 목록 → 상점에서 사고 돌아와도 그대로였다 | 목록을 닫고 결과 시트로 돌아갈 때 다시 쓴다 |
| 버저가 안 보임 | 0초에 공이 날고 있다는 표시가 시간 자리의 회색 12px "버저!"뿐이었다. 소리를 끄면(🔇) 다른 신호가 없었다 | 버저 동안 시간 글씨를 빨갛게, 세 번 통통 튀게(모션 줄이기면 색만). 빨강은 글씨용 `--red-ink`(#D22030)다. 막대·점에 쓰는 `--red`(#F04452)는 회색 바탕에서 3.4:1이라 12px 글씨로는 흐리다. 저장 안내("이 브라우저에선…")도 같은 색. 지금은 회색 바탕 4.8:1, 흰 바탕 5.3:1 |
| 버저 중 아이템을 누르면 "공이 돌아오면 쓸 수 있어요" | 0초에 날던 공을 끝까지 보게 되면서 생긴 상태다. 비행 중 안내를 그대로 띄웠는데, 0초 뒤로는 공이 다시 안 오고 곧 "시간이 끝났어요"가 떴다 | 시간이 0이면 먼저 "시간이 끝나서 더 못 써요". 공 +2 도 안 쓰인다 |
| 키보드로 오류 시트 뒤의 ☰에 닿음 | 시트의 막(`.veil`)이 화면을 덮어 손가락·마우스로는 뒤를 못 누르는데, Tab 으로는 헤더 버튼에 갔다. 오류 시트에서 ☰ → 닫기를 누르면 지난 판의 실패 시트("공이 다 떨어졌어요")가 떴다 | 시트가 하나라도 떠 있으면 그 뒤(헤더·코트·아이템 막대 등)를 `inert`·`aria-hidden`으로 둔다. 알림은 시트 위에도 떠야 해서 뺐다 |
| 알림이 낱말 가운데서 두 줄로 끊김 | `left:50%`에 폭을 안 줘서 알림이 화면 절반(390에서 195px)에 갇혔다. 열두 글자만 넘어도 두 줄이 되고 "돌려드렸 / 어요"처럼 끊겼다 | 글자 폭에 맞추되 화면 폭 −32px까지 늘어나고, 줄은 낱말 단위로 바꾼다. 그랬더니 공유가 안 되는 브라우저에서 띄우는 주소(띄어쓰기 없음)는 320px에서 상자 밖으로 삐져나갔다(글자 폭 340, 상자 288). 한 줄에 안 들어가는 낱말만 가운데서 끊는다(`overflow-wrap:anywhere`) |
| 사생활 보호 모드에서 별·코인이 조용히 사라짐 | 사파리 사생활 보호 모드 등은 읽기는 되고 쓰기에서만 예외를 낸다. 게임은 되는데 아무것도 안 남았다 | 켤 때 한 번 써 보고, 안 되면 시작 화면에 "이 브라우저에선 기록이 저장되지 않아요". 320×568에선 이 한 줄 때문에 시작 시트가 넘쳐 맨 아래 버튼 줄(상점·게임 방법)을 스크롤해야 보인다. 쓰기가 막힌 브라우저와 가장 작은 화면이 겹칠 때만이라 그대로 뒀다 |
| **360px 폰에서 연속 칩이 뜨면 코트가 줄었다 늘었다 함** | 목표 칩 줄(골·동전·연속·점수)이 줄바꿈을 허용했다. 360·320px 폭에선 2연속째 `🔥 연속` 칩이 붙는 순간 두 줄(36→72px)이 되고, 그만큼 코트가 줄었다가 연속이 끊기면 다시 커졌다. 360×640에서 코트 폭 260→237px, 320×568에서 215→193px. 끄는 거리(화면 px)를 코트 단위로 바꿔 힘을 재니 같은 길이를 끌어도 힘이 10~11% 세졌다. 한창 연속으로 넣고 있을 때 손의 감이 바뀐 셈이다 | 칩 줄을 **한 줄·높이 36px 고정**(칩 29 + 여백 7, 예전 한 줄 높이 그대로라 넓은 폰에선 코트가 안 바뀐다)으로 두고, 연속은 칩에서 빼서 **코트 위 가운데 배지**로 옮겼다. 배지는 (210, 30) 가운데, 튈 때까지 쳐서 코트가 300px 이상이면 x 155~265·y 14~46 안이고, 글자를 키우는 좁은 폰에선 320×568 결승(코트 206px, 두 자리 연속)에서 x 136~284·y 10~50까지 커진다. 이 상자엔 소품이 없다(바람 화살표는 양 끝 x 26~40·380~394). 375px 미만에선 칩 안팎 여백만 줄이고 이름(골·스윔·동전·점수)은 그대로 보인다. 연속 칩이 빠진 뒤로는 320px 결승(칩 3개)에 네 자리 점수 `1,234`를 넣어도 칩이 76/88/79px, 288px 중 253px 라 자리가 남는다. 처음엔 375px 미만이면 목표 칩 이름을 무조건 숨겨서 360px 에서도 `🏀 0 /5  🪙 0 /3`만 보이고 줄 오른쪽 140px 안팎이 비었다. 스윔 판은 `✨ 0 /3`만 남아 무엇을 세는지 안 보였다. 지금은 칩을 새로 쓸 때와 창 크기가 바뀔 때 **이름까지 들어가는지 재 보고**(`fitChips`, 점수 칩이 잘리는지를 본다), 안 들어갈 때만(280px 폰 결승의 네 자리 점수, 글자가 넓은 글꼴, 글자 키움) 목표 칩 이름을 화면에서만 1px 로 접는다. `display:none`으로 숨기면 화면 읽기에서도 빠져 크롬 접근성 트리에 스윔 판이 `✨ 0 /3`로만 남는다(`textContent`엔 글자가 있어서 검사는 통과했다). 점수 칩은 아이콘이 없어서 이름까지 숨기면 맨숫자만 남아 바로 아래 '남은 공 N'의 개수처럼 읽힌다(타임어택에선 화면의 유일한 숫자). 그래서 점수 칩은 안 접는다. 줄 높이는 접든 말든 36px 라 코트는 그대로다. 그래도 모자라면 점수 칩만 줄어들고, 넘친 칩은 가로로만 잘린다(`overflow:clip`이 없는 iOS 15 웹뷰 등은 위아래도 자르는 `hidden`이라, 칩이 튈 때 윗변이 안 잘리게 아래 여백 2px 를 위로 옮긴다. 줄 높이는 그대로). 배지는 처음엔 공 위에 그렸는데, 넣은 슛 7개 중 하나꼴(14%, 봇 격자 1,244골)로 높은 포물선 꼭대기에서 공이 배지 밑으로 0.1초쯤 사라졌다. 지금은 공·조준 점선보다 먼저 그린다 |
| 280px 폰 결승에서 공 +2 를 쓰면 코트가 줄어듦 | '남은 공 N'을 줄바꿈 금지 없이 점 옆에 붙였다. 280×653에서 점이 16개를 넘으면(결승 14개 + 공 +2) 자리가 모자라 이 글자가 두 줄이 되고, 공 줄이 22→34px 로 커져 결승 코트가 256→251px 로 줄었다(판 도중에 같은 길이를 끌어도 힘이 2% 셈). 이 패키지 전부터 있던 문제인데, 예전엔 힌트 줄 높이에 남는 자리가 있어서 코트가 안 줄었다. `fitHint`가 280px 에선 모든 판을 두 줄 높이(46px)로 잡으면서 그 여유가 없어져 드러났다. 320px 이상은 점 18개와 글자가 한 줄에 들어가서 안 바뀐다 | '남은 공 N'은 줄을 안 바꾸고, 대신 점이 줄어든다(280px 에서 18개면 9→7.5px). 점 높이를 폭에서 받아서(`aspect-ratio`) 줄어도 동그랗다. 280·320·360px 에서 공 8~30개 모두 공 줄 22px, 코트 폭 그대로, 가로 넘침 없음 |
| **좁은 폰에서 아이템을 켜면 코트가 줄었다 늘었다 함** | 아이템을 켜면 힌트 줄이 판 힌트에서 `🧲 자석 켜짐 · 림 근처까지 온 공을 가운데로 끌어당겨요`로 바뀐다. 힌트 줄은 높이가 정해져 있지 않아서, 문구 길이에 따라 한 줄(31px)과 두 줄(46px)을 오갔다. 360×640 바람 판에선 아이템 4종 모두(판 힌트가 두 줄이고 켠 문구는 한 줄) 코트 폭 266→275px, 320×568에선 자석을 켜면 바람 판을 뺀 모든 판(11판 + 타임어택)에서 한 줄→두 줄이 되어 231→221px(결승 215→206px), 바람 판에선 나머지 3종을 켜면 거꾸로 221→231px. 던지면 아이템이 꺼지고 힌트가 돌아와 코트도 돌아갔다. 조준하는 동안만 같은 길이를 끌어도 힘이 3~5% 달랐던 셈이다. 칩 줄을 고정한 것과 같은 문제인데 힌트 줄은 그대로였다 | 판을 시작할 때(그리고 창 크기가 바뀔 때) 그 판에서 뜰 수 있는 문구 5개(판 힌트 + 켤 수 있는 아이템 4종)를 실제로 넣어 재 보고, **가장 긴 것의 높이로 힌트 줄을 잡아 둔다**(`fitHint`). 판 안에선 힌트 줄이 안 바뀌어 코트도 안 바뀐다. 줄이 바뀌는 폭은 글꼴마다 달라서(여기 검사 환경은 WenQuanYi, 폰은 Noto Sans·Apple SD Gothic Neo 등) CSS 로 폭을 정해 두지 않고 잰다. 대가: 320px 에선 자석 문구가 두 줄이라 모든 판이 두 줄 높이로 잡혀 코트가 231→221px(결승 215→206px)로 한 단계 작다. 360px 이상은 판 힌트가 두 줄인 바람 판만 두 줄이고, 나머지 판은 예전 크기 그대로(390×844는 전부 366px). 줄은 낱말 사이에서만 바꾼다(`word-break:keep-all`. 예전엔 320px 자석 문구가 `끌어당겨 / 요`, 360px 바람 판 힌트가 `있어 / 요.`로 한 글자만 다음 줄로 넘어갔다). 그리고 320px 에선 한 줄 힌트가 두 줄 높이로 잡힌 줄의 맨 위에 붙어 아래 16px 이 비었다. 지금은 세로 가운데(위 8·아래 9px). 코트 크기는 둘 다 안 바뀐다 |
| 좁은 폰에서 캔버스 글자가 5~7px | 코트를 화면에 맞춰 줄이니 코트 단위 글자도 같이 줄었다. 320×568에서 황금 공 표시(12단위)가 6.0px, 연속 칩으로 코트가 더 줄면 5.5px. 360×640에서도 7.3px | 캔버스 글자(득점 `+N`·부제, 황금 공 표시, 방울 아이콘, 연속 배지)는 화면에서 **10px 밑으로 안 내려가게** 코트 단위로 되돌려 키운다(`fontPx`). 코트 폭이 350px 이상이면(키 큰 390px 폰 등) 그대로다. 390×664처럼 키 작은 폰은 코트가 290px 라 조금 커진다. 커진 글자가 코트 밖으로 잘리지 않게 득점 글자와 황금 공 표시는 안쪽으로 민다 |
| 작은 안내 글씨가 흐림 | 힌트·부제·남은 공·남은 시간·상점 설명 같은 11~12px 안내에 장식용 회색 `--ink-3`(#8B95A1)을 썼다. 회색 바탕 2.8:1, 흰 바탕 3.0:1 | 안내 글씨는 `--ink-3s`(#6B7684, 회색 바탕 4.2:1, 흰 바탕 4.6:1). 목표 칩의 `/5`, 실패 시트의 상점 권유도 같은 색. 잠긴 칸·못 사는 버튼처럼 비활성 표시는 흐린 회색 그대로 |
| 시작 화면에 켜 두기만 해도 초당 60번 다시 그림 | 루프가 판이 진행 중이든 아니든 매 프레임 코트를 그렸다. 시트(시작·클리어·일시정지…)가 떠 있으면 물리가 안 돌아 코트는 그대로인데, 흐린 막 뒤에서 같은 그림을 1초에 60번(실측 61번) 다시 그렸다 | 시트가 떠 있고 판이 진행 중이 아니면 **한 번만** 그리고 멈춘다(실측 1초에 0번). 시트를 열고 닫거나 화면 크기가 바뀌면 한 번 다시 그린다. 그리기가 터지면 다 그린 것으로 치지 않고 다음 프레임에 다시 그린다. 그래야 매번 터지는 경우 예전처럼 오류 시트까지 간다. 캔버스 해상도도 기기 배율 2.5배에서 **2배**로 낮췄다. 3배 폰에서 픽셀이 36% 줄고 선·글자 차이는 거의 안 보인다 |
| 칩이 튀는 효과가 한 번도 안 쓰임 | `.chip.pop` 애니메이션은 CSS 에 있었지만 붙이는 코드가 없었다 | 칩을 다시 쓸 때 **보이는 숫자가 바뀐 칩만** 한 번 튀긴다(골을 넣으면 골·점수, 동전을 주우면 동전·점수). 처음엔 실제 값으로 비교해서 골 3/3을 채운 뒤 더 넣으면 3/3 그대로인 칩이 튀었다. 지금은 칩에 보이는 값(목표에서 멈춘 값)으로 비교한다. 새 판을 시작할 땐 지난 값을 비워서 칩이 전부 튀지 않는다 |

효과 입자는 전부 `spark()`(불꽃, 최대 140개)와 `pushPop()`(득점 글자, 최대 8개)으로만 넣는다. 꽉 차면 가장
오래된 것(곧 사라질 것)부터 버린다. 바람 줄은 24개, 불공 꼬리는 30개에서 멈춘다(평소엔 각각 12개, 18개 안팎).
바람 줄이 꽉 차도 위치·길이 난수는 예전처럼 뽑고 버린다. 같은 씨앗으로 예전 코드와 나란히 돌리는 비교가 여기서 어긋나지 않게.
효과를 얹을수록 한 프레임에 그리는 양이 늘어나서, 폰에서 그리기만으로 프레임이 밀리지 않게 상한을 먼저 뒀다.
시트는 전부 `role="dialog"`·`aria-modal`이고 시트의 제목(h2)을 이름으로 쓴다. 알림은 `aria-live="polite"`, 코트 캔버스는
`role="img"`(농구 코트).

득점 판정은 공의 **직전 위치와 현재 위치**로 림 평면을 위에서 아래로 가로질렀는지 본다.
한 프레임에 최대 40단위까지 움직이므로 위치만 보면 빠른 공이 림을 지나쳐 버린다.
림 양 끝은 반지름 5짜리 원 두 개, 백보드와 가로막은 사각형으로 두고 가장 가까운 점을
찾아 밀어내는 방식으로 푼다.

## 검증

헤드리스 크로미움에서 실제로 돌렸다.

**물리** — 12스테이지 × 26샷 무작위(312샷)에서 코트 이탈 0, NaN 0, 공이 갇히는 경우 0,
워치독이 끊은 정상 슛 0. "갇힘"은 워치독 상한(8초)과 다음 공 대기(0.5초)를 넘겨도 공이 살아
있는 것이다. 예전엔 300스텝(5초)으로 잘랐는데, 움직이는 가로막 위를 구르다 떨어지는 정상 슛이
그보다 길 때가 있어서(예전 코드로도 24,000샷 중 5번, 최장 374스텝) 이 항목이 가끔 헛실패했다.
이 검사는 판을 시작하자마자 던져서 막대가 늘 출발 위치에 있다. 이 조건에선 워치독이 정상 슛을 끊지
않아 100번 돌려 100번 통과했지만(31,200샷), 막대가 아무 위치에 있는 실제 플레이에선 드물게 끊는다(위 표).
자석·얼음·불공을 켠 채로도 각각 168샷씩 같은 조건으로 돌려 이탈·NaN 0.
가로막이 있는 스테이지도 넘겨서 넣는 길이 열려 있다(6·10·12번에서 각각 28·28·31개 조합).
동전은 재도전하면 다시 생긴다.

**아이템** — 얼음은 골대·가로막을 정확히 멈추고 바람을 0으로 만든다. 불공은 가로막을
뚫고, 점수는 불공 2배·황금 공 3배·둘 다 6배로 정확히 곱해진다. 켜기만 해선 줄지 않고
던지는 순간 하나 쓰며, 다음 공엔 남지 않는다. 0개면 켜지지 않고, 비행 중엔 켤 수 없다.
방울을 맞히면 아이템이 하나 들어온다.

**조작·화면** — 스테이지 목록의 잠금과 해제, 시작 안내, 실제 스와이프로 슛, 남은 공 감소,
목표를 채우면 클리어 시트와 별 저장, 공을 다 쓰면 실패 시트, 다시하기, 제한 시간 있는
판의 타이머 표시, 나가기 확인까지 실제 이벤트로 눌러서 확인했다(23항목). 아이템 쪽은
상점 구매와 코인 차감, 켜기/끄기, 스와이프로 던지면 소모, 공 +2, 클리어 코인 보상,
별 3개 첫 달성 선물(두 번째는 없음), 동전 → 지갑, 실패 화면의 아이템 권유, 새로고침 후
아이템·지갑 유지까지 30항목.

`--virtual-time-budget` 으로 돌리면 `requestAnimationFrame` 이 1500ms 동안 한 프레임밖에
안 돌아서 게임 루프가 통째로 검증되지 않는다. 시계·비행·일시정지처럼 루프가 있어야 하는
것들은 CDP 로 진짜 브라우저를 붙여 실시간으로 확인했다(node 22 내장 WebSocket, 외부 패키지 없음).

**안정성** — 시간 끝 판정·버저비터·NaN·워치독을 스텝 단위로 43항목(`stable.html`): 결승에서
골 뒤 0.5초 대기 중 0초 → 클리어, 공을 든 채 0초 → 다음 스텝에 끝, 타임어택 버저비터는 득점하고
시간은 0 그대로, 결승 버저 실패는 공이 떨어진 뒤에 실패, NaN 공은 환불되고 동전을 안 먹음,
8초 비행과 백보드 위·얼음으로 선 막대 위에 얹힌 공은 워치독이 끊고, 움직이는 막대 위에 얹힌 공은
안 끊음. 게임의 프레임 함수를 붙잡아 34·70ms 늦은 프레임을 직접 넣어 보면 타임어택 코인은 한 번,
결승 실패 시트도 한 번, 판이 끝난 스텝 뒤로는 골대가 안 움직임. 버저 중 NaN 환불 뒤 "버저!"가 안 남음.
시간을 남기고 깬 결승은 스텝이 더 돌아도 보상 한 번. NaN 환불은 던질 때 쓴 아이템도, 황금 공도 그대로 돌려줌.
캔버스 `fillText`와 `AudioParam.setValueAtTime`을 가로채 적어 두고, 버저비터가 들어간 뒤 그린 화면에 "버저비터!"가 있고
0초 스텝에 버저 두 음(220·277Hz)이 판 전체에서 한 번, 들어간 스텝에 불 소리(520Hz)가 나는지 타임어택과 결승에서 본다
(상태값만 보면 문구나 소리가 빠져도 통과해서). 버저 중·결론 대기 중 아이템은 "시간이 끝나서 더 못 써요", 1스테이지
백보드 윗면을 천천히 굴러 들어가는 슛 3개는 골. 이 패키지 전 코드로는 7항목만 통과한다.
실제 브라우저로는 80항목(`cdp4`): 클리어·실패·타임어택 결과에서 연 목록을 닫으면 그 시트로,
시작 안내의 뒤로(목록·클리어·시작 세 갈래), 안내를 열어도 하던 판 번호가 안 바뀜, 물리·그리기에
예외를 한 번씩 넣어도 골대가 계속 움직이고 공중에서 없어진 공은 알림으로 알림, 계속 터지면 오류 시트와
다시하기·처음으로, 그리기가 매 프레임 터지면 다시하기 뒤·시작 화면에선 새로고침을 권하고 시트는 한 번만
만들며 새로고침하면 새 문서의 시작 화면이 뜨고 오류 시트로 안 돌아감, 두 번째 손가락·오른쪽
버튼으로는 못 던짐, 떼기를 놓친 조준 포인터가 다시 누르면 새 기준점, 미리 누른 손가락이 조준으로
이어짐(미리 누른 마우스 왼쪽을 오른쪽을 누른 채 뗐으면 안 이어짐), 조준 중 목록을 열면 조준을 놓음, 실제 루프에 40·70ms 멈춤을 넣어도 타임어택 코인은 한 번,
버저 중 시간 글씨가 빨강이고 아이템을 누르면 "시간이 끝나서 더 못 써요", 상점에 다녀온 결과 시트의 지갑·권유가 새것,
390·320px에서 알림이 한 줄이고 공유 주소 알림은 상자 안에서 줄바꿈, 오류 시트가 떠 있으면 헤더·아이템 막대가 `inert`라
`focus()`가 안 되고 닫히면 다시 됨,
`localStorage` 쓰기를 막으면 저장 안내가 뜨고 게임은 끝까지 됨. 끝의 9항목은 `PointerEvent`를 직접 만들지 않고
CDP로 터치(에뮬레이션)·마우스를 넣어 브라우저가 `pointerId`를 붙이는 입력 경로로 두 손가락·미리 누름·오른쪽 버튼과
왼쪽+오른쪽 동시 누름(왼쪽을 뗄 때 던지고, 오른쪽을 마저 떼도 더 안 던짐)을 다시 본다. 이 패키지 전 코드로는 16항목만 통과한다.

**좁은 폰·그리기** — 실제 브라우저로 71항목(`cdp5`). 360×640(3배)과 320×568(2배)로 에뮬레이션해 결승에서 2연속을
넣는 동안 코트 높이·칩 줄 높이가 그대로이고 가로 넘침이 없음, 칩에 "연속"이 없고 코트 위 가운데(210, 30)에 주황 배지
`🔥 2연속`을 그림, 캔버스 `fillText`를 가로채 그 사이 그린 글자(득점·황금 공 표시·방울·배지 등 약 330번)가 화면에서 전부
10px 이상, 캔버스 픽셀이 CSS 폭의 2배 이하. 힌트·부제·남은 공·남은 시간 글씨 색, 시트 11개의 `role`·제목 연결, 알림의
`aria-live`. 골을 넣으면 골·점수 칩이 튀고 바뀐 게 없이 다시 칠하면 안 튀며, 동전을 주우면 동전·점수 칩만 튐.
불꽃 500개를 넣어도 140개, 점수 글자 20개를 넣어도 8개이고, 꽉 찬 채로 골을 넣어도 넘지 않음. 그린 횟수는 게임이 센 값과
코트 캔버스의 `clearRect` 호출 수 둘 다로 본다. 시작·일시정지·클리어 시트 뒤에선 1초에 0번, 게임 중엔 60번, 시트를
바꾸거나 화면 크기가 바뀌면 한 번. 360·320px 에서 13판 × 아이템 4종을 켜고 끄는 동안, 그리고 켜고 던진 뒤에도 코트 폭이
그대로임. 390×664에서 힌트 자간을 넓혀(글자가 넓은 글꼴 흉내) 같은 검사. 360·320px 에서 네 자리 점수를 넣어도 칩 이름(골·스윔·
동전·점수)이 다 보이고 점수가 안 잘림. 280×653 결승에서 공 +2 를 네 번 써도 코트 폭·공 줄 높이가 그대로이고 '남은 공 N'이 한 줄,
점 18개가 7.5px 로 줄어도 동그람. 같은 폭에서 칩 자간을 넓히면 목표 칩 이름만 접고(점수 칩은 안 잘림, 접힌 이름도 접근성 트리엔
남음), 자간을 되돌리고 창 크기 이벤트를 보내면 다시 폄. 골 3/3을 채운 뒤 더 넣으면 골 칩은 안 튐. 배지 한가운데(210, 30)에 공을 두고 일시정지하면 시트 뒤 한 장에서 배지를
먼저, 공을 나중에 칠함. 그 한 장이 지금 판(배지·공 있음)이고, 연속 중에 나가면 시작 화면 뒤 그림엔 배지·공이 없음. 림 맞고
넣은 2연속의 득점 글자가 `2연속!`. 칩 이름은 접근성 트리(`Accessibility.getFullAXTree`)에 골·스윔·동전이 그대로 남음(5·11·12판).
배지 글자의 변환 배율로 크기를 재서, 2연속·3연속이 되는 순간 1.18배로 튀었다가 1배로 돌아오고(1초에 39장), 빗나가면 배지가
사라지고, 연속 중에 판을 다시 시작하면 배지도 칩 튐도 없음. 코트 캔버스 이름이 `농구 코트 · 2연속`으로 바뀌고 끊기면 돌아옴.
'동작 줄이기'(`prefers-reduced-motion`)를 에뮬레이션하면 배지가 처음부터 1배. 바람 판에서 난수를 0.01로 묶어 매 스텝 바람 줄을
만들면 30스텝 뒤 24개에서 멈추고 200스텝 뒤 그린 줄도 23개(상한 없던 코드는 390줄), 꽉 찬 스텝도 난수를 3개 씀. 불공 꼬리는 30개
이하(실측 18개). 힌트는 1·4판 × 아이템 4종 문구 모두 낱말 사이에서만 줄이 바뀌고, 한 줄 힌트는 줄의
세로 가운데. 지금의 71항목으로 돌리면 이 패키지 전 코드는 7항목만 통과한다(시작 시트 뒤에서 1초에 61번 그림). 첫 리뷰 반영 전
코드는 43항목(아이템을 켜면 360 바람 판 266→275px, 320px 에선 15곳에서 코트 폭이 바뀜), 두 번째 리뷰 반영 전 코드는 51항목(칩 이름이
접근성 트리에서 빠짐, 힌트가 `끌어당겨 / 요`처럼 낱말 안에서 끊기고 320px 한 줄 힌트가 줄 맨 위에 붙음), 세 번째 리뷰 반영 전 코드는
55항목(360·320px 에서 칩 이름이 숨음, 280px 결승에서 공 +2 로 코트 256→251px, 캔버스 이름에 연속 없음, '동작 줄이기'에도
배지가 튐, 꽉 찬 바람 줄이 난수를 1개만 씀, 바람 줄·꼬리 수를 보는 훅 없음).

**확인 안 된 것** — 실기기 터치 패널(두 손가락 입력은 합성 이벤트와 CDP 터치 에뮬레이션으로만 봤다), 사람의 실제
플레이 감각. 난이도는 봇 기준이라 사람이 하면 다를 수 있다. 캔버스 컨텍스트 손실은 이벤트만 걸어
두었고 실제 GPU 리셋으로 일으켜 보지는 못했다. 그리기가 매 프레임 터지는 경우도 테스트 훅과 `arc`를 바꿔 끼워
흉내 낸 것이다. 새로고침으로도 안 고쳐지는 기기라면 이 게임은 할 수 없다. 사파리 사생활 보호 모드도 실기기에서는 안 봤다.
늦은 프레임은 바쁜 대기와 붙잡은 프레임 함수로 흉내 낸 것이고, 실제 30Hz 절전 기기에서는 안 봤다.
시트 뒤 `inert`는 `focus()`로만 봤다. 실제 Tab 키 이동과 스크린 리더로는 안 봤다. 시트의 `role="dialog"`·알림의
`aria-live`도 속성만 확인했고 스크린 리더가 실제로 읽는지는 안 들었다. 좁은 폰에서 접은 칩 이름도 크롬 접근성 트리로만 봤고
TalkBack·VoiceOver 로 듣지는 않았다. 캔버스 이름의 연속(`농구 코트 · 3연속`)은 알림(`aria-live`)이 아니라서
연속이 바뀔 때 저절로 읽히지는 않고, 코트로 옮겨 갔을 때 읽힐 것으로 보는데 이것도 실제로 들어 보지는 않았다. `overflow:clip`이 없는 브라우저는 `@supports` 조건을 바꾼 사본으로 흉내만 냈다. 360·320px 폰은 에뮬레이션으로만 봤다. 폰 설정에서
글자를 키운 경우 칩 줄(높이 36px 고정)이 어떻게 보이는지는 안 봤다. 힌트 줄 높이는 재서 잡지만, 실제로 잰 글꼴은 이 검사
환경의 WenQuanYi 와 자간을 넓힌 흉내뿐이다. 폰의 한글 글꼴(Noto Sans·Apple SD Gothic Neo)에서 몇 줄이 되는지는 안 봤다.
'동작 줄이기'를 켜도 연속 배지는 바뀔 때 0.18초 커졌다 돌아온다(칩의 CSS 애니메이션만 멈추고, 캔버스 쪽은 아직 안
막았다). 버저·불 소리는 주파수를 설정했는지만 봤고 귀로 듣지는 않았다.

## 앱인토스

이 게임에는 앱인토스 SDK 브릿지가 없다. 형제 게임(`games/tikkeul-moa/`)에는 붙어 있고,
필요해지면 같은 어댑터를 그대로 가져오면 된다(리더보드 점수 제출은 **문자열**, 종료는
`Screen.close()`, 식별키는 `User.getAnonymousKey()`).

정적 웹에서 만족시킬 수 있는 가이드 항목은 그대로 지켰다. 외부 요청 0건, 사운드·햅틱
On/Off 유지, 백그라운드 전환 시 소리 즉시 종료(`AudioContext.suspend`)와 일시정지,
Safe Area 미침범, 모든 화면에서 종료 가능 + 종료 확인 모달, 세로 고정, 브라우저 히스토리
미조작, `eval`/외부 스크립트 없음, 깬 스테이지·별·아이템·코인은 재접속해도 유지.

## 라이선스

개인 프로젝트. 실제 구단·리그의 이름이나 로고는 쓰지 않았다.
